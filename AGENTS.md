# 项目调查笔记

本目录存放用于调查、学习和记录各个项目的笔记。

## 写作要求

- 将本目录中的任务视为学习型调研和知识记录。
- 重点说明项目的实现、设计和可观察到的行为；使用中立、描述性的表述。
- 可以记录调查中发现的问题。说明问题的依据、表现和影响；区分已确认的事实与推测。
- 即使记录问题，也不要将调查笔记写成代码审查、PR 审查或修改建议。
- 除非用户明确要求，不提供问题优先级、修复方案、补丁或 PR 修改建议。
- 新建或实质更新单项目笔记前，先阅读所在类别目录的 `调查指南.md`。指南是覆盖检查表，不要求机械保留所有标题；合并或省略章节时，应说明相关能力不存在、超出范围还是尚未验证。

## 调查深度

默认采用“标准调查”，保证同类别笔记能够横向比较：

1. 给出结论摘要和系统边界。
2. 说明一条完整主链路，包括入口、状态、执行、输出或持久化。
3. 回答类别指南中的必查问题。
4. 记录设计取舍、已确认的边界和未验证事项。
5. 给出能够定位结论的源码依据。

“扩展调查”用于用户明确要求的专项，或对理解项目确有必要的复杂机制。主题、无障碍、动画、桌面集成、极端性能等通用 UI 审计项，应放在“扩展调查”章节，不默认扩写。扩展内容较长且可独立阅读时，拆成单独笔记，并在主笔记中链接。

篇幅由机制复杂度决定，不设统一字数。不得用更多小标题、重复结论或重复证据制造表面上的完整性。

## 证据与结论

- 重要结论优先在正文就近标注“相对路径 + 符号名或行号”。文末源码索引只收录关键入口，避免重复抄写正文的全部引用。
- 区分三类表述：源码直接确认的事实、基于静态代码的推断、尚未运行或尚未覆盖的事项。
- 声称“没有某能力”时，写明检查范围和搜索依据。仅仅没有在已读入口中看到，应写“本次未找到”，不能写成项目级绝对结论。
- 发现文档、注释与实现不一致时，同时列出两者，并以当前代码快照中的可执行路径为实现依据。
- 调查 UI 行为时，静态代码只能确认入口、状态和事件绑定；视觉效果、键盘可用性、性能与平台行为需要运行验证。

## 内联代码与技术细节组织

内联代码用于标识必须逐字准确的技术对象，不用于一般强调。正文应先让读者看懂机制，再提供足以复查结论的源码定位。是否给单个名称加反引号属于建议性选择；达到下文的复查条件后，逐段检查和重新组织内容属于写作验收要求，不能只删除反引号而保留原有的符号清单。

### 适合保留内联代码的内容

- 首次出现且会影响理解或检索的源码符号、配置键、协议字段、状态字面量、命令、路径和短小源码定位。
- 结论依赖其精确拼写或取值差异的对象。例如区分 `pending` 与 `queued`，或说明 `metadata.isQueued` 的实际持久化语义。
- 读者可能据此回到源码继续调查的关键入口。源码定位尽量合成一个完整片段，如 `src/services/session.ts:120-168`，不要把文件名、符号名和行号拆成多段标记。

### 优先改为普通文字的内容

- 项目名、产品概念、通用技术名词和已经解释过的对象。英文词不因使用拉丁字母就需要内联代码。
- 同一段中反复出现的同一符号。首次写出精确名称后，可改称“发送入口”“该状态”“消息服务”等不会混淆的中文短称。
- 不影响结论的辅助函数、局部变量、完整调用参数和成串状态值。先概括它们共同完成的动作，只保留决定机制边界的名称。
- 为同一项事实重复给出的符号名、文件名和行号。相邻结论来自同一处实现时，可在句末或段末合并为一次定位。

### 先检查段落承担的功能

- 一个自然段或一个列表项应主要回答一个问题，例如“请求如何路由”“权限在哪里判断”或“结果如何持久化”。注册来源、执行链、审批、安全边界、配置、生命周期等多个主题并列出现时，应按主题拆段。
- 第一句先给出机制判断或可观察结果，后文再补条件和证据。若段落必须依赖一串函数名才能让读者猜出结论，应重写自然语言叙述。
- 去掉反引号后仍然连续出现大量英文符号、文件名、参数和值，说明问题是实现清单过密，不能视为已经完成优化。
- 同一段落列出多个平行对象，却没有解释它们之间的来源、顺序、条件或主次关系时，应改成列表、表格、流程或源码索引。

### 必须专项处理的内容

- **横向对比中的单项目概览**：只保留共同比较维度上的结论和关键差异，例如循环所有者、审批方式、执行位置与结果预算。完整注册来源、调用链和函数清单留在单项目笔记；横向对比不得重写一遍单项目实现摘要。
- **白名单、枚举和字面量集合**：正文解释这些值用于什么判断，并只举足以说明边界的例子。三个以上值需要完整保留时，改用列表、表格或代码块，不在句中连续堆叠内联代码。
- **状态矩阵与安全配置**：涉及多种模式、条件、权限和结果时，按“场景/条件/行为/边界”拆成列表或表格。字段精确性是保留值的理由，不是把整个矩阵写成一个长段落的理由。
- **多条源码入口**：正文保留一个主要入口和一个紧邻证据即可。其余入口移到本节末尾的小型源码列表或文末“关键源码索引”。
- **版本、提交和能力变化清单**：先概括这一组变化共同说明什么；逐项提交号确有追溯价值时使用列表，不把多个提交、功能名和路径压在一句话中。

### 密度判断

- 普通正文每句话通常保留一至两处内联代码即可。达到三处时，必须复查这句话是否在用符号代替叙述。
- 一个自然段或列表项累计达到六处内联代码时，必须检查段落功能并优先拆分；达到十处时，除非该段本身就是短小的协议或字段对照，否则应改成列表、表格、代码块或“关键源码索引”。
- 一个自然段出现四个以上源码定位时，应合并定位或把次要入口移到源码索引。不要为每个动作分别附一组“符号名 + 文件名 + 行号”。
- 一句话需要列出三个以上符号时，先判断读者是否真的需要完整清单。只需理解机制时改为中文概括；确需逐项查阅时使用结构化形式。
- 修改时依次考虑删除无关名称、合并重复定位、把细节移到专门列表。不要为了减少数量而删掉支撑结论所必需的字段差异或源码证据。
- 完成一段后可暂时忽略反引号重读：如果句子离开符号名就无法说明“谁做了什么、何时发生、产生什么结果”，应先补足自然语言叙述。

以上数量用于触发复查，不用于机械删词。协议、字段契约和源码索引可以保留较多精确名称，但仍须采用适合枚举或对照的结构，不能继续写成高密度长段落。

### 施工与验收

- 施工时按完整自然段检查，不按 Markdown 物理行或单个反引号逐项替换。列表中的每个项目也视为一个自然段。
- 对所有触发密度条件的段落，至少完成一种实质处理：概括机制、拆分主题、合并定位、移动清单或改为结构化表示。仅去掉代码样式、把符号改成普通英文或调整换行不算完成。
- 复核横向对比时，确认每个单项目段落都在回答统一比较问题。删除源码符号后若仍像单项目笔记的压缩版，应继续删减或改写。
- 复核安全、协议和配置主题时，确认“条件导致什么行为”在普通文字中可以直接读出，完整值清单与源码证据作为后置材料。
- 交付前搜索本次修改段落中含三处以上内联代码的句子，并人工复查含六处以上内联代码的段落；允许保留的例外应能明确说明其对照或检索用途。

### 示例

不建议：

> `sendMessage()` 检测 `isSessionGenerating(sessionId)`；命中后把 `sessionId` 加入 `queuedSessionIds`，调用 `chatHandler.sendMessage(..., {skipGeneration: true})`，并写入 `metadata.isQueued = true` 和 `status = "queued"`。

建议：

> 发送入口若发现同一会话仍在生成，就先持久化新消息并标记为排队，等当前回合结束后再触发后续生成；判断、入队和落库逻辑见 `sessionGenerationManager.ts:279-344`。

如果调查主题正是排队状态的数据契约，则保留必要字段即可：

> 排队状态由 `metadata.isQueued` 与 `status` 共同表达，旧数据中的 pending 状态只在展示层兼容。

完整白名单不应继续塞在正文句子中。正文可以写：

> Desktop Push 只接受文档声明、常见语义容器、绘图元素、样式和目标指令等前缀；完整前缀集合见下列清单，流式发送另有节流与空闲超时。

随后用列表或代码块保存确需逐项核对的字面量。

## 推荐通用结构

```md
# <项目> <类别>调查笔记

> 调查对象：...
>
> 调查更新日期：...
>
> 代码快照：...
>
> 调查方式：...
>
> 调查范围：...
>
> 文档定位：实现学习与跨项目横向比较，不作为整改方案

## 结论摘要
## 系统边界与总体调用链
## <类别必查主题，按项目实际结构组织>
## 设计取舍与已确认边界
## 扩展调查（可选）
## 未验证事项
## 关键源码索引
```

横向对比只使用各单项目笔记共同覆盖的维度。“项目不支持”“笔记未调查”和“无法确认”必须使用不同标记。新增项目笔记后，应检查同类别横向对比的对象列表是否需要更新。

## 开头元数据

新建或实质更新调查文档时，在一级标题后立即写入元数据引用块。字段名、顺序和标点必须与下列模板一致；每个字段单独占一行，使用全角冒号，并在相邻字段之间插入一行空引用标记 `>`，使元数据保持在同一个引用块内，同时避免 Markdown 渲染时压缩换行。信息无法确认时保留字段并填写“未确认”，不要省略字段、合并字段或自行改名。

单项目调查笔记使用：

```md
> 调查对象：`<项目名称或远端链接>`
>
> 调查更新日期：YYYY-MM-DD
>
> 代码快照：`<完整 commit SHA>`（分支：`<branch>`）
>
> 调查方式：<使用的主要证据和调查方法>
>
> 调查范围：<本次覆盖的主题与明确排除项>
>
> 文档定位：实现学习与跨项目横向比较，不作为整改方案
```

横向对比文档使用：

```md
> 对比对象：<项目列表>
>
> 对比更新日期：YYYY-MM-DD
>
> 依据：<单项目调查笔记、代码快照或其他证据>
>
> 对比方法：<统一的比较维度和方法>
>
> 对比范围：<本次覆盖的主题与明确排除项>
>
> 文档定位：实现学习与跨项目横向比较，不作为整改方案
```
