# 检索增强与认知编排横向对比

> 对比对象：VCPToolBox、AIO Hub、Dify、Open WebUI、Chatbox、Jan、AstrBot、Cherry Studio、DeepChat、Hermes Agent、LobeHub、SillyTavern、Risuai、VCPChat
>
> 对比更新日期：2026-08-31
>
> 依据：同目录十四份以各项目当前源码快照直接调查的单项目笔记
>
> 对比方法：先按长期事实对象、检索循环所有者和结果契约划分谱系，再在局部比较摄取、候选、写回、作用域、预算、观察与恢复；不进行质量、延迟或成熟度排名
>
> 对比范围：知识资产、相似度召回、上下文即时注入、工具化检索、检索驱动认知编排和记忆维护；普通上下文压缩、网页搜索和客户端事件展示不单独视为检索算法
>
> 文档定位：实现学习与跨项目横向比较，不作为整改方案

## 结论摘要

这些项目不是一条从“简单 RAG”升级到“高级 RAG”的单轴序列。Dify、Open WebUI、Chatbox、AIO Knowledge 与 AstrBot 维护文档资产；Cherry Studio、DeepChat、Hermes Agent、LobeHub 进一步表明工具化召回的事实源可以是知识库、执行轨迹、跨会话历史或结构化用户记忆；SillyTavern、Risuai 及 AIO Recall 在发送前选择文本；VCPToolBox 既把检索产物作为后续阶段的查询方向和可维护的认知资产，也以 OneRing 维护跨端近期事实并可注入其摘要。评价时应先保持这些边界。

1. **VCPToolBox 有两条应分开看的 RAG 生效路径，也是本组唯一已确认的检索驱动认知编排实现。** 常规路径由 system 中的命名日记本占位符触发：当前用户/assistant 意图形成查询向量，在指定一个或多个日记本范围内做 KNN、可选 BM25/时间/语义组、TagMemo 或 RiverMemo，结果直接替换占位符并注入本轮上下文。思维簇路径则是元思考链：前一簇命中的向量与原查询融合，作为下一簇的查询；它才构成阶段反馈。DailyNote 是会话内把新事实写入日记本并交给异步索引的常用写入入口，不应把“日记本检索”缩减为写回。维护者部署观察显示约 98% 日记来自主动或指导式 DailyNote 写入；Dream 占比低，却会生成独立的梦境叙事和联想内容。代码快照中的 Dream manifest 为 `.block`，故该运行状态属于已启用的具体部署，不能外推为默认安装或从文件数推断来源比例。详见 [VCPToolBox 单项目笔记](VCPToolBox-检索增强与认知编排调查笔记.md)。
2. **知识资产管线的重心在生命周期与治理。** Dify 维护租户级 Dataset、Document、分段、异步索引、发布版 Pipeline 和运行记录；Open WebUI 维护知识库、文件、集合、外部知识连接和授权；Chatbox 与 AIO Knowledge 在本地持久化文件、chunk 和索引。它们都能有混合检索、重排或工作流，但当前主链的默认产物仍是本次问题的候选内容或来源。
3. **工具循环不自动构成查询规划。** Chatbox、Jan 和 Open WebUI 把工具结果回送给模型，模型可再查询或读块。AIO Knowledge research 还有由命中、缺口和粗略冲突触发的固定 search/read 循环。它们的循环所有者分别是模型或规则，并未确认有固定的“结果向量生成下一阶段查询”协议。
4. **发送前选择与历史记忆是另一组问题。** AIO Recall、SillyTavern World Info/Vector Storage 和 Risuai Lorebook/记忆引擎决定本轮携带哪些条目或历史。Risuai Hypa V3、SillyTavern Data Bank 和 AIO Recall 均有多信号候选或扩展，但输出仍是 prompt 文本，不是连续认知路径。
5. **记忆写回必须与检索分开比较。** Open WebUI 的 Persistent Memory 可由工具或默认关闭的后台复盘直接写入用户集合；SillyTavern 摘要随新消息写回聊天；Risuai 记忆压缩写回聊天字段；VCPToolBox 的 DailyNote 是会话内显式工具写入。其 Dream 在实际启用部署中是低频的独立梦境叙事和联想链，后续变更仍须管理员审批。写回存在不代表检索已成为认知编排。
6. **VCPChat 是远端能力的客户端观察和操作面。** 它能请求日记搜索、呈现远端关联结果、显示 RAG/元思考事件和发送审批响应，但当前仓库不实现远端的摄取、Embedding、排序或元思考。
7. **新增正式样本扩展了“检索对象”而非认知编排定义。** AstrBot 将每库 SQLite/FAISS/FTS 资产以默认注入或模型工具消费；Cherry Studio 以 fail-closed 的知识库范围向模型提供 search/read；DeepChat 让模型在当前或直属已完成子 Agent 的 Tape 中 search-then-read；Hermes Agent 将 profile 文件记忆、外部 provider 预取和会话 FTS 分开；LobeHub 将后台提取的五层用户记忆作为可读写工具事实。它们均未在已调查主链中确认固定的结果到下一阶段查询协议。
8. **OneRing 不能被遗漏为日记 RAG 的附属功能。** 它按 Agent 维护短期 user/assistant 事实账本，并可将近期内容压缩为 OneRingMemo 注入顶层 system；这与 DailyNote 的文件知识资产、元思考的向量阶段反馈都不同。当前 manifest 与处理器的静态 record-only 默认值不跨端补齐，静态代码不能把它描述为已实际运行的跨端合并体验。

## 谱系地图

```text
长期资料与索引
  |- 知识资产管线：Dify、Open WebUI、Chatbox 知识库、AIO Knowledge、AstrBot、Cherry Studio
  |- 模型工具化检索：Chatbox、Jan、Open WebUI、AIO Knowledge、AstrBot、Cherry Studio、DeepChat、Hermes Agent、LobeHub
  `- 相似度/混合候选：上述资料库，以及 AIO Recall

发送前上下文选择
  |- AIO Recall（占位符和 binding）
  |- SillyTavern（World Info、摘要、Vector Storage/Data Bank）
  |- Risuai（Lorebook、聊天记忆压缩和历史语义选择）
  `- VCPToolBox OneRing（近期事实账本与 OneRingMemo 摘要）

命名日记本 RAG 与连续认知方向
  `- VCPToolBox（日记本占位符的当前轮检索：KNN/TagMemo/RiverMemo；思维簇元思考链的阶段反馈）

长期记忆写入与维护
  |- VCPToolBox（DailyNote 会话记录与异步索引；可选 AgentDream）
  |- Hermes Agent（显式文件记忆与可选 provider 回合同步）
  `- LobeHub（五层用户记忆的后台提取与工具写回）

远端消费与观察
  `- VCPChat
```

谱系可以重叠。例如 Open WebUI 同时有知识库、native 检索工具和用户记忆；AIO 同时有 Recall 与 Knowledge。图中归类描述调查到的执行链，不表示产品全部功能，也不表示质量排序。

## 一览：主链与结果契约

| 项目                                                     | 主链类型                                   | 循环或选择的所有者                         | 本轮最终产物                     | 结果反馈为后续查询                         |
| -------------------------------------------------------- | ------------------------------------------ | ------------------------------------------ | -------------------------------- | ------------------------------------------ |
| [VCPToolBox](VCPToolBox-检索增强与认知编排调查笔记.md)   | 命名日记本 RAG、认知编排、长期维护及 OneRing 近期事实 | 日记本路径由占位符/检索管线；元思考为固定阶段；OneRing 按 Agent 账本/摘要选择 | 本轮日记候选、近期事实或摘要；或结构观察和下一阶段查询方向 | 仅元思考已确认：融合前一思维簇向量         |
| [AIO Hub](AIO-Hub-检索增强与认知编排调查笔记.md)         | Recall 注入、知识资产检索和工具化 research | Recall 预设管线；research 固定规则         | prompt 文本或工具结果            | 本次未确认向量阶段反馈                     |
| [Dify](Dify-检索增强与认知编排调查笔记.md)               | 知识资产检索                               | 检索节点；后续由用户工作流图决定           | context 或结构化节点结果         | 取决于用户定义的工作流，不归入组件内建反馈 |
| [Open WebUI](Open-WebUI-检索增强与认知编排调查笔记.md)   | 知识资产检索、工具化检索和用户记忆         | legacy 单轮检索；native 工具循环由模型控制 | RAG context 或工具消息           | 本次未确认固定阶段反馈                     |
| [Chatbox](Chatbox-检索增强与认知编排调查笔记.md)         | 知识库/附件检索和模型工具调用              | 模型工具循环                               | 工具结果；附件可继续读取父块     | 模型可继续行动，未确认固定反馈协议         |
| [Jan](Jan-检索增强与认知编排调查笔记.md)                 | 本地资料库工具检索                         | 模型工具调用                               | 工具输出和引用卡                 | 模型可继续行动，未确认固定反馈协议         |
| [AstrBot](AstrBot-检索增强与认知编排调查笔记.md)         | 每库文档资产的混合检索、默认注入或工具检索 | 请求装配；agentic 时由模型工具循环         | 临时用户内容或工具结果           | 模型可继续行动，未确认固定反馈协议         |
| [Cherry Studio](Cherry-Studio-检索增强与认知编排调查笔记.md) | 本地知识库的 search/read 工具链         | 模型工具循环；有效 Base 范围 fail-closed   | MCP 工具结果                     | 模型可继续行动，未确认固定反馈协议         |
| [DeepChat](DeepChat-检索增强与认知编排调查笔记.md)       | Session Tape 的 search-then-context         | 模型工具循环                               | 紧凑候选或受预算证据             | entry ID 驱动读取，不是固定查询反馈        |
| [Hermes Agent](Hermes-Agent-检索增强与认知编排调查笔记.md) | 文件/外部记忆预取与跨会话 FTS 工具        | 请求装配、provider 生命周期或模型工具循环  | system 快照、用户 sidecar 或工具结果 | 未确认固定反馈协议                       |
| [LobeHub](LobeHub-检索增强与认知编排调查笔记.md)         | 五层用户记忆的后台提取和工具检索/写回       | 工作流提取；运行时由模型工具循环           | 记忆工具结果                     | 模型可继续行动，未确认固定反馈协议         |
| [SillyTavern](SillyTavern-检索增强与认知编排调查笔记.md) | 发送前上下文选择和聊天维护                 | 规则扫描或一次近邻查询                     | prompt 槽文本或重排历史          | 未确认                                     |
| [Risuai](Risuai-检索增强与认知编排调查笔记.md)           | Lorebook 与聊天记忆选择                    | 规则递归；每次发送择一记忆引擎             | system 文本                      | 未确认                                     |

该表只比较主链的控制关系和结果契约。长期保存什么、如何写回或重建，分别见后续表格；VCPChat 是远端能力的消费端，单列于“远端能力消费与可观察性”。

## 局部比较

### 知识资产检索

| 比较对象       | 摄取与持久化                                     | 候选形成                           | 返回位置                     | 作用域与授权                      | 运行记录                           |
| -------------- | ------------------------------------------------ | ---------------------------------- | ---------------------------- | --------------------------------- | ---------------------------------- |
| Dify           | tenant 隔离队列、阶段状态与发布 Pipeline         | 关键词、向量、全文、混合和跨库处理 | 应用 context 或节点结果      | tenant 与 Dataset 权限            | 查询审计、trace、Pipeline/节点执行 |
| Open WebUI     | 文件状态、向量化任务、修复与外部连接             | 向量、BM25 hybrid 与重排           | legacy RAG 模板或来源事件    | 所有者、组和 AccessGrants         | 事件、日志和 source 事件           |
| Chatbox 知识库 | 本地数据库、索引和主进程常驻 worker              | 向量 top-20，可选 rerank 至 top-5  | 本地会话所选知识库的检索结果 | 本地会话选择的单库                | 日志及附件开发面板                 |
| AIO Knowledge  | 每资料库 SQLite、文档/chunk、lease 和前端 worker | FTS/BM25、向量、邻块和跨域 RRF     | 工具结果或 research 输出     | Agent `knowledgeAccess` allowlist | trace、来源、进度和降级理由        |
| AstrBot        | 每库 SQLite 块表、FAISS、FTS5 与元数据 SQLite | 稠密/稀疏融合、去重及可选 rerank    | 临时上下文或 `astr_kb_search` | 会话选库；Dashboard `kb` scope     | 上传进度、手动检索和 debug 日志    |
| Cherry Studio  | Base、条目、chunk、Concept 与可恢复索引任务    | `kb_search` 后可按 Concept 深读     | `kb_search`/`kb_read` 工具结果 | 绑定及冻结 Composer Base 范围       | 任务恢复；查询细节未在本轮展开      |

这六者可比较资料的可编辑性、索引恢复、混合候选、来源契约和授权位置。不能只按“是否支持 rerank”排序：Dify 的 Pipeline 强调摄取发布，Open WebUI 还接入外部库，Chatbox 的知识库目前是桌面端单库，AstrBot 以每库本地索引和会话选库为边界，Cherry Studio 则把有效 Base 范围强制为工具可见性的前提。

### 模型工具检索

| 比较对象          | 发现对象                  | 读取粒度                       | 工具循环                                | 特有预算                                   |
| ----------------- | ------------------------- | ------------------------------ | --------------------------------------- | ------------------------------------------ |
| Chatbox           | 绑定知识库或本回合大附件  | chunk；附件可再读父块          | AI SDK 继续步骤                         | 附件召回 20、返回最多 12                   |
| Jan               | 当前 thread/project 附件  | chunk；可按范围读块            | tool output 自动续发                    | 默认 top-k 3                               |
| Open WebUI native | 有权限且绑定的知识库/文件 | 语义片段、单文件或受限文件工具 | 通用工具循环最多 256 次                 | 工具迭代上限和文件工具输出上限             |
| AIO Knowledge     | Agent allowlist 内资料库  | search、chunk read、research   | 模型工具调用；research 内部另有规则循环 | research 默认 3 轮、12 次读取/查询、120 秒 |
| AstrBot           | 会话或全局选定知识库      | 融合块                          | agentic 模式由模型调用 `astr_kb_search` | 最终数量默认 5；工具循环预算属通用 runner |
| Cherry Studio     | 有效 Base scope           | 命中 Concept 与原文区段         | 模型调用 search/read/list/manage          | 空范围不公布工具且直调拒绝                 |
| DeepChat          | 当前 Tape 或直属 finalized 子 Tape | entry 概览、邻近证据      | 模型调用 `tape_search` 后 `tape_context`   | 搜索默认 20、最多 50；上下文默认 16 KiB    |
| Hermes Agent      | profile 会话库或外部 provider | FTS 命中窗口、滚动/读取结果 | 模型调用 `session_search`                 | FTS 扫描最多 300 候选；外部预取 8 秒        |
| LobeHub           | 用户/workspace 的五层记忆 | 语义或分类记忆记录              | 模型调用 `lobe-user-memory`               | 工作流限流；候选/阈值未在本轮确认           |

它们共同的事实是“模型可基于前一工具结果再行动”。静态代码不能保证模型会形成稳定计划。AIO 的 research 虽有补充查询，但补充条件是固定规则，不应同 VCPToolBox 的向量阶段反馈混同。

### 发送前上下文选择

| 比较对象                     | 长期或聊天级事实              | 激活/候选                           | 本轮注入位置               | 本轮内的边界                   |
| ---------------------------- | ----------------------------- | ----------------------------------- | -------------------------- | ------------------------------ |
| VCPToolBox 命名日记本 RAG     | 按日记本划分的文件、chunk、标签和向量索引 | system 占位符限定范围；向量、可选 BM25/时间/语义组、TagMemo 或 RiverMemo | 占位符替换后的 system 或虚拟 system 消息 | 单轮候选注入；只有元思考另有跨簇向量反馈 |
| AIO Recall                   | 完整条目和带权标签            | 关键词、内容向量、标签邻近/共现     | 格式化条目替换占位符       | 扩展当前候选，不形成新模型回合 |
| Hermes Agent 外部记忆         | provider 管理的 profile 记忆  | 当前用户消息的 provider 预取        | 当前用户 API sidecar       | 单轮预取；具体候选由 provider 决定 |
| SillyTavern                  | Lorebook、摘要、聊天/附件向量 | 关键词/规则递归或 KNN               | prompt 槽和历史重排        | 规则扫描或单次近邻查询         |
| Risuai                       | 三源 Lorebook、聊天摘要/历史  | 关键词/正则递归；摘要相似度         | system 文本                | 每次发送只运行一个记忆引擎     |
| Open WebUI Persistent Memory | 用户事实和路径化 context 记忆 | 用户消息窗口的语义 top-8 与路径邻域 | system 中的 memory context | 当前路径不组织思维簇阶段       |
| VCPToolBox OneRing | 按 Agent 隔离的 user/assistant 原始消息及可选 Memo | 顶层 system 中的 OneRingMemo 占位符读取已持久摘要；主插件可按时间与 hash 补齐近期原始块 | 顶层 system 替换摘要；或 message 数组中的补齐块 | 默认 record-only 不补齐；摘要不是向量检索或阶段反馈 |

这些实现都能改变本轮 prompt，部分还会写回长期或聊天级状态。VCPToolBox 的命名日记本路径与它们同样以当前轮候选注入为终点，差异在于日记本是显式的文件/索引范围，并可接入 TagMemo 或 RiverMemo；不能因为同属 VCP 就把该路径直接计为思维簇阶段反馈。OneRing 也只提供近期原始事实或摘要：它不查向量、不从命中向量构造下一阶段查询，默认 record-only 下甚至不补齐跨端原始块。AIO 的标签扩散、SillyTavern 的 World Info 递归和 Risuai 的 Lorebook 递归只扩大当前候选或扫描缓冲；它们不产生新的模型回合或由候选向量决定的下一阶段检索。Open WebUI 的后台复盘是主动写回，但它不把检索结果组织成思维簇阶段。

### 记忆写回与恢复

| 项目/机制                    | 触发时机                      | 写入对象               | 确认或自动化边界                              | 后续可用方式                     | 恢复或一致性边界                         |
| ---------------------------- | ----------------------------- | ---------------------- | --------------------------------------------- | -------------------------------- | ---------------------------------------- |
| VCPToolBox DailyNote         | 会话内显式工具调用            | 日记及其索引管线       | 维护者部署中约 98% 的日记来自主动或指导式记录 | 经 FIFO 落盘和后台索引后参与检索 | 后台处理；本次未验证故障一致性           |
| VCPToolBox Dream             | 实际启用部署中的低频后台链路  | 独立梦境叙事和联想资产 | 管理员审批后才写回                            | 写回后进入相关资产链             | 跨文件操作非原子                         |
| VCPToolBox OneRing           | 每次带主触发的请求及最终响应  | 按 Agent 的近期消息账本 | 默认 record-only；AI 回复经异步 hook 写入     | 主插件可补齐；Memo 可替换 system 占位符 | 消息裁剪与 turn/快照清理不一致；异步任务不跨重启恢复 |
| Open WebUI Persistent Memory | API、工具或默认关闭的后台复盘 | 每用户记忆集合         | API/工具直写；后台复盘为可选自动路径          | 用户消息窗口的语义召回           | 集合可重建                               |
| SillyTavern 摘要             | 新消息后                      | 聊天摘要               | 随聊天处理自动写入                            | 后续 prompt 组装                 | 随聊天保存；向量索引可由原文重建         |
| Risuai 记忆压缩              | 发送时                        | 聊天字段               | 发送路径自动压缩                              | 后续 system 记忆文本             | Embedding 缓存独立；本次未找到后台整理链 |
| AIO Recall                   | 用户或 Agent 工具操作         | Recall 条目及派生索引  | 显式写入                                      | 后续占位符替换                   | SQLite 与派生索引可重建                  |
| Hermes Agent 内置/外部记忆    | 显式 memory 工具；或 provider 回合同步 | profile 文件或 provider 后端 | 内置写入可审批；外部同步异步串行 | 下会话系统快照、下轮预取或工具检索 | provider 细节与队列完成度未验证 |
| LobeHub Personal Memory       | 用户分析、小时工作流或模型工具 | 五层用户记忆及向量索引 | 工作流按层提取；工具可读写 | 后续 Agent 工具回合 | 多来源并发一致性未验证 |

写回表比较跨轮形成或修改事实的路径，不以“是否自动”替代检索能力。VCPToolBox 的 Dream 是独立内容链，不能与 DailyNote 合并为单一整理器。

### 运行约束与结果预算

| 项目/路径              | 候选或返回预算         | 循环、读取或阶段预算                    | 超时、取消或终止边界                     | 结果裁剪位置                |
| ---------------------- | ---------------------- | --------------------------------------- | ---------------------------------------- | --------------------------- |
| Chatbox 附件           | 召回 20，返回最多 12   | 由 AI SDK 工具循环继续步骤决定          | 本次范围未确认独立时限                   | 工具结果；可继续读取父块    |
| Jan                    | 默认 top-k 3           | 模型按需调用 retrieve、list、get_chunks | 本次未找到固定循环或迁移主链             | 原生工具输出                |
| Open WebUI native      | 各文件工具另有输出上限 | 通用工具循环最多 256 次                 | 迭代上限                                 | tool message 与 source 事件 |
| AIO Knowledge research | 由资料库候选策略决定   | 默认 3 轮、12 次读取/查询               | 120 秒或固定轮次/次数                    | research 输出               |
| VCPToolBox 元思考      | 多路候选覆盖和拓扑排序 | 预设思维簇阶段                          | 以阶段协议为边界；具体运行预算未在此汇总 | 下一阶段查询方向和结构观察  |
| VCPToolBox OneRing      | 每 Agent 默认保留 100 条；Memo 近 3 天、不足取 30 条 | 热配置最多补 10 个消息块；Memo 输入/输出默认 32k/2k token | Only 异步落库与 AI hook 均不阻塞上游响应 | 原始消息块或 Memo 文本 |

这些数值描述当前调查到的实现约束，不构成不同项目的性能比较；候选数相同也不表示候选语义、重排或最终注入策略相同。

### VCPToolBox：日记本 RAG 与检索驱动认知编排

VCPToolBox 的常规日记本 RAG 是一条独立的当前轮注入链。system 或虚拟 system 消息中的日记本占位符限定一个或多个日记本；插件以最新真实用户消息和最近 assistant 消息的加权向量为查询，在该范围内形成候选，并可选用历史主题分段、时间、BM25、语义组、TagMemo、RiverMemo、重排和去重。结果带来源路径，替换原占位符进入本轮 prompt。这里的“日记本”既是文件的一层目录/索引作用域，也是用户或角色可在提示词中选择的检索对象；它不是 DailyNote 工具本身。见 [VCPToolBox 单项目笔记的“占位符到查询向量”和“请求入口、候选与输出主链”](VCPToolBox-检索增强与认知编排调查笔记.md#占位符到查询向量)。

元思考链才是本轮唯一能确认“检索结果改变后续查询方向”的主链：每一预设思维簇从上一簇命中向量的均值与原查询融合出的向量继续检索。RiverMemo 为日记本 RAG 提供多路候选覆盖、拓扑排序和结构观察，但单独启用 RiverMemo 不会把结果变成下一阶段查询。比较认知编排时，应要求同样的阶段状态、反馈契约、终止条件和路径解释；本组其他项目目前没有该同类实现。

DailyNote 解决的是会话中显式记录、文件提交和后续异步索引衔接，是日记本事实持续更新的常用入口；它不等于日记本查询。OneRing 则记录近期对话事实，不产生日记 chunk、Embedding 或 TagMemo/RiverMemo 派生资产；它与 OneRingMemo 是发送前连续性层。维护者的部署观察进一步显示，Dream 是低频但内容独立的梦境叙事和联想链，而不只是整理器。

### 远端能力消费与可观察性

| 客户端                                           | 可请求的远端能力               | 可显示的信息                    | 可操作动作             | 无法由客户端确认的事项              |
| ------------------------------------------------ | ------------------------------ | ------------------------------- | ---------------------- | ----------------------------------- |
| [VCPChat](VCPChat-检索增强与认知编排调查笔记.md) | 日记搜索、联想、聊天或工具请求 | RAG、元思考、关联结果和审批事件 | 发送审批响应、取消搜索 | 摄取、Embedding、排序和远端索引恢复 |

客户端显示的信息来自远端返回。观察器可重连，但这不证明事件或远端状态可恢复。

## 已确认边界与未验证事项

- 全部单项目结论以静态源码为主。VCPToolBox 另补充维护者的单一部署观察和目录统计：约 98% 日记来自会话内主动/指导式记录，五类思维簇共有 32 个文件，且存在独立梦目录；这些不是统一的召回率、引用正确性、延迟、成本、并发或用户体验 benchmark，也不能由文件数反推写入来源。
- “本次未发现查询反馈”均限定于各笔记列出的入口和搜索范围，不能作为项目或生态的绝对否定。用户可借 Dify Workflow、模型工具调用或插件自行组成额外循环，这不等于其检索组件内建该循环。
- 资料中的提示注入隔离普遍未运行验证。多个项目会将检索文本直接交给模型；来源、引用或权限字段能帮助追溯，不能单独证明内容安全。
- 权限的比较应区分服务端租户/ACL、桌面本地 collection、Agent allowlist 与 UI 过滤。VCPChat 隐藏文件夹、Jan thread scope 或 Chatbox 当前会话附件都不是 Dify/Open WebUI 式多租户授权模型。
- 写回的比较应区分：聊天摘要覆盖、用户事实直接写入、会话内显式工具编辑与人工审批后的后台文件变更。VCPToolBox 的 DailyNote 是将事实写入日记本的路径，而日记本占位符是从这些事实形成当前轮 RAG 的路径；二者不能互相代称。维护者观察表明 DailyNote 是约 98% 的主来源；Dream 是低频但独立的梦境内容链。该比例与内容体验是单一部署的运行事实，其他项目的可逆性、冲突和故障一致性仍须分别验证。
- OneRing 的可比较范围是跨端近期事实记录、编辑识别、摘要注入和异步恢复，不是知识库摄取、向量召回或认知链。当前静态默认的 `ONERING_RECORD_ONLY=true` 与明确 Only 模式都不做跨端补齐；是否有部署关闭该默认值、实际补齐准确性和多前端时间线效果仍未运行验证。

## 单项目笔记索引

- [VCPToolBox](VCPToolBox-检索增强与认知编排调查笔记.md)
- [AIO Hub](AIO-Hub-检索增强与认知编排调查笔记.md)
- [Dify](Dify-检索增强与认知编排调查笔记.md)
- [Open WebUI](Open-WebUI-检索增强与认知编排调查笔记.md)
- [Chatbox](Chatbox-检索增强与认知编排调查笔记.md)
- [Jan](Jan-检索增强与认知编排调查笔记.md)
- [AstrBot](AstrBot-检索增强与认知编排调查笔记.md)
- [Cherry Studio](Cherry-Studio-检索增强与认知编排调查笔记.md)
- [DeepChat](DeepChat-检索增强与认知编排调查笔记.md)
- [Hermes Agent](Hermes-Agent-检索增强与认知编排调查笔记.md)
- [LobeHub](LobeHub-检索增强与认知编排调查笔记.md)
- [SillyTavern](SillyTavern-检索增强与认知编排调查笔记.md)
- [Risuai](Risuai-检索增强与认知编排调查笔记.md)
- [VCPChat](VCPChat-检索增强与认知编排调查笔记.md)
