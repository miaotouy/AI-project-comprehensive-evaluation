# 产品结构与设计基因类目设计记录

> 记录日期：2026-08-18
>
> 状态：已建立首版目录、调查指南与 AIO-Hub 样本；AIO-Hub 能力汇总和汇总指南已按新口径修订
>
> 讨论范围：单项目汇总失真、来源笔记覆盖缺口、产品画像、能力入口、设计基因和工程证据边界

## 背景

随着单项目调查笔记增多，“已调查能力汇总”开始承担两种不同任务：一是索引已经调查的功能，二是让脱离本地工作区的读者快速形成产品画像。现有结构更擅长前一项。

在把 AIO-Hub 汇总交给没有本地源码和讨论背景的网页 AI 阅读后，出现了两类稳定误读：

1. AIO 被解释为以 Chat/Agent 为中心、其他工具附属于 Agent 的 AI 客户端；
2. web-canvas 等局部模块的具体问题被外推为“AIO 整体工程实现极其脆弱”。

进一步反查发现，误读并非只由汇总措辞造成。汇总是已有来源笔记的二次压缩，来源层本身也缺少产品演变、完整入口地图和普通人类工具的统一承接位置。

## 反查结论

### 汇总压缩丢失了已有信息

AIO-Hub 独特功能笔记已经明确记录多项能力的入口：正则工作台、Git 工作台、窗口自动化、实时字幕 OCR、长文翻译和弹幕播放器以用户手动操作为主，部分明确没有 Agent facade；网页蒸馏和媒体生成则是人机双入口。

现有汇总大多删除“入口与触发者”，保留实现机制，又把统一工具系统整体放到“Agent 运行时与外部协作”下。读者因此难以看出 Agent 只是部分能力的调用入口。

### 来源类目遗漏了普通人类工具

现有类目存在一条结构性空隙：

- Agent 工具只调查模型可发现和调用的能力；
- 独特功能只调查有产品辨识度的完整工作流；
- Chat、媒体、上下文等专项各有明确主题；
- 普通但完整、主要由人直接操作的工具没有统一类目。

这使 API Tester、配置转换、代码格式化、系统仪表盘、符号链接工具等能力只能进入“第二梯队”或“无隐藏特性”清单，没有完整用户主链。它们不够独特，不表示它们不属于产品。

### 产品起点和演变没有来源笔记承接

AIO-Hub 的 Git 历史显示：2025-07-24 先初始化多功能 Tauri 工具应用并加入正则、对比和格式化等工具；2025-10-16 才加入 LLM Chat；2026-02-17 加入 Tool Calling；2026-07-09 Agent Manager 才形成独立工具边界。

现有笔记没有要求调查这种形成顺序，汇总只能读取当前 README 和当前模块体量，容易把后期最复杂、最显眼的 AI 能力误判为产品原点。

### 工程证据没有统一比较口径

web-canvas 的八项结构性断点是经过深入静态调查确认的子系统结论。其他来源同时记录了会话原子写、revision、备份轮换、损坏隔离、路径沙箱和 343 个测试文件等工程证据。

现有文档没有项目级可靠性调查，也没有保证所有项目、所有模块接受同等强度的风险检查。因此：

- 局部断点不得外推为全项目可靠性结论；
- 问题记录较少不得解释为其他项目更可靠；
- 静态源码主链不得直接等同于经过运行验证的稳定交付。

## 本次决议

### 新建“产品结构与设计基因”类目

目录位置：[`产品结构与设计基因/`](产品结构与设计基因/)

该类目负责：

- 产品起点、关键演变和当前定位；
- 主要用户、调用者和产品表面；
- 能力的组织单位、接入骨架和共享事实源；
- 人类、Agent、插件、后台任务、外部节点和其他设备的入口关系；
- 跨越多个功能并长期影响产品扩张方式的设计基因；
- 现有专项无法承接的产品表面和调查缺口。

该类目不替代独特功能。独特功能回答“有哪些难以替代的完整能力”，产品结构与设计基因回答“这些能力为什么能以这种方式共同存在，以及整个产品怎样继续生长”。

### 设计基因必须分层取证

首版指南采用六种标记：`作者自述`、`历史确认`、`当前结构确认`、`跨模块归纳`、`候选基因`、`阶段性/已失效`。

这样可以避免以下混淆：

- 当前代码结构被改写成作者原意；
- 当前营销口号覆盖产品前史；
- 模块接入 Agent 较深被解释为产品以 Agent 为中心；
- 多个局部实现被包装成没有反例的“产品 DNA”。

### 汇总概览应优先消费本类目

后续修订“已调查能力汇总”时，项目概览应首先读取产品结构与设计基因笔记，并保留：

- 产品起点与当前重心的差异；
- 产品表面和主要调用者；
- Agent 在整体结构中的位置；
- 能力组织骨架；
- 局部风险不能代表全项目的证据边界。

功能条目还应保留必要的入口标记，例如“人类 UI”“Agent”“双入口”“内部基础设施”“外部协议”，不能只保留实现名称。

## 首个样本：AIO-Hub

已创建 [AIO-Hub 产品结构与设计基因调查笔记](产品结构与设计基因/AIO-Hub-产品结构与设计基因调查笔记.md)。首版确认：

1. 多功能桌面工具应用早于 Chat 和 Agent；
2. registry 与统一工作区构成能力接入骨架；
3. 人类 UI、内部服务、Agent、插件和外部节点是并列入口；
4. 复杂能力经常形成可直接操作的工作台；
5. 本地事实源与远程模型、外部节点分层；
6. “微创新累积式扩张”暂列候选基因，不冒充作者已经确认的稳定原则。

当前快照有 47 个 registry 文件，18 个文件包含明确的 `agentCallable: true`。该统计用于证明 registry 不等于 Agent 工具，不作为最终 Agent 方法数量或功能完成度统计。

## 后续工作

已完成：

- 依据 AIO-Hub 新笔记修订其“已调查能力汇总”的项目概览、入口地图、功能分组和工程证据边界；
- 修改汇总指南中“静态源码贯通即视为完成交付态”的过强口径，将源码主链、测试证据、运行验证和外部依赖拆开；
- 在汇总指南中加入调查深度说明，禁止把单一子系统的问题密度外推为项目级可靠性评价。
- 精简汇总指南，取消固定章节顺序、强制完成度速览和按调用者数量触发入口地图等格式要求，避免把 AIO-Hub 的产品特性耦合成所有项目的规范。

仍待处理：

1. 为普通人类工具设计覆盖方式：可以建立“工具工作台与通用实用工具”类目，也可以先在产品结构笔记中维护完整入口清单，再按自然聚类拆分类目。
2. 从 VCPChat、Open WebUI、Cherry Studio 等产品表面较多的项目开始补写本类目，验证指南是否需要调整。
3. 逐步修订其他项目汇总中的旧证据口径；在来源笔记未补齐前，不机械批量替换项目画像。

## 本次产物

- [产品结构与设计基因调查指南](产品结构与设计基因/调查指南.md)
- [AIO-Hub 产品结构与设计基因调查笔记](产品结构与设计基因/AIO-Hub-产品结构与设计基因调查笔记.md)
- [修订后的 AIO-Hub 已调查能力汇总](已调查能力汇总/AIO-Hub-已调查能力汇总.md)
