# 候选分类方向调查

> 对比对象：现有 22 个项目及其已调查主题、独特功能候选聚类
>
> 对比更新日期：2026-08-31
>
> 依据：`README.md`、`独特功能/待查清单.md`、各主题调查指南与横向对比、各项目独特功能及已调查能力汇总
>
> 对比方法：先按共同用户目标、事实对象、生命周期和证据完整度筛选候选，再检查与现有类目的重叠；不以功能名称相似或项目数量直接判定应新建类目
>
> 对比范围：跨项目产品能力的候选分类方向；不重新调查单项目源码，不评价实现质量，不把机制贡献表直接变成产品类目
>
> 文档定位：实现学习与跨项目横向比较，不作为整改方案

## 结论摘要

当前候选方向中，主动 Agent 与后台任务已完成第一轮正式横向调查：六个项目可按独立日程运行、会话内续作、条件唤醒和进程内后台工作分型比较。协同工作区仍是下一轮优先候选；进一步调查时仍需把触发、执行、结果归属和恢复语义分开记录。

上下文编译与提示词工程已经达到正式类目的准入条件：AIO Hub、SillyTavern、Risuai、VCPChat 和 VCPToolBox 都有可持久化的规则、模板、宏或占位符，并能追踪到模型请求或显示投影。该类目应从对话请求与上下文中拆出，但按结果去向分为请求层编译、消息生命周期变换和显示层渲染，不把“正则”单独升格为顶层类目。

记忆演化与用户建模、多表面与远程连续性具有比较价值，但分别与检索增强、会话管理和外部协作存在明显交叉。建议先形成边界说明或局部专题，不立即增加顶层目录。Agent 社会与群体可以作为多 Agent 协作的聚类入口，但现有样本同时包含群聊、委派、论坛和 IM 投影，需先统一比较对象。

研究数据发布、轨迹生成与会话档案经复核不构成同质类目：Pi、Hermes Agent、OpenCode 的对象和消费者不同，应保留为对话导出与分享中的研究/发布分型及项目独特能力标签。模型评估与竞技目前只有 Open WebUI 一个已确认样本，暂不具备横向类目条件。人类可操作工具面更像 Agent 工具、Chat UI 和生成式运行时的交叉观察维度，不建议独立建类。

## 判定标准

候选方向进入正式类目，至少需要满足以下条件：

1. **目标相近**：项目为相近的用户或调用者目标提供连续工作流，而不是仅调用同一种 API。
2. **对象相近**：存在可识别的任务、文档、轨迹、记忆或会话等事实对象，并能说明其权威源。
3. **生命周期相近**：至少能比较创建、执行、更新、完成、失败、取消或恢复中的多项状态。
4. **样本充分**：至少三个项目有主链或接近主链的证据；只有一个项目时保留为能力卡。
5. **边界可解释**：能说明它与已有类目为何不同，且不会把普通 Chat、单次工具调用或纯工程机制重复计入。

证据状态沿用各来源笔记。`主链确认`只表示当前快照中可追踪到入口、状态、执行和结果交接；没有运行验证的视觉、平台、性能和生产行为仍列为未验证事项。

## 候选方向矩阵

| 候选方向 | 样本覆盖 | 共同事实对象 | 与现有类目关系 | 当前判断 |
| --- | --- | --- | --- | --- |
| [主动 Agent 与后台任务](主动Agent与后台任务/主动Agent与后台任务横向对比.md) | VCPChat、VCPToolBox、Hermes Agent、Open WebUI、LobeHub、AstrBot | 非用户即时输入触发的运行、任务记录与结果归属 | 与生成运行时、外部协作、会话管理相邻，但关注触发和后台生命周期 | **已独立成类** |
| 上下文编译与提示词工程 | AIO Hub、SillyTavern、Risuai、VCPChat、VCPToolBox | 可持久化的规则、模板、宏、占位符及其编译后的请求或显示投影 | 与对话请求、消息渲染器、检索增强相邻，但关注规则对象、编译顺序和消费面 | **建议独立成类** |
| 协同工作区 | VCPChat、LobeHub、Open WebUI、Cherry Studio、OpenCode | 文档、项目、文件或页面及其编辑状态 | 与生成式输出、Chat UI、仓库分布相邻，但关注人和 Agent 对同一对象的共同操作 | **建议独立成类** |
| 研究数据发布、轨迹生成与会话档案 | Pi、Hermes Agent、OpenCode | 分别是研究数据、批量轨迹或可移植会话档案 | 对象、消费者和生命周期不同；对话导出指南已包含 E5“发布与研究” | **不独立成类，保留分型** |
| 记忆演化与用户建模 | VCP 系、Hermes Agent、LobeHub、Open WebUI、SillyTavern | 用户事实、长期记忆、摘要或可检索认知资产 | 与检索增强与认知编排高度重叠 | **先做边界专题** |
| 多表面与远程连续性 | Hermes Agent、OpenCode、DeepChat、AstrBot、VCPChat、Open WebUI | 跨界面或平台共享的会话/事件流 | 与会话管理、外部协作高度重叠 | **先做扩展专题** |
| Agent 社会与群体 | VCP 系、LobeHub、AstrBot、DeepChat、Hermes Agent | 多参与者会话、委派关系、群组或论坛对象 | 与 Agent 角色、工具、外部协作交叉 | **先统一分型** |
| 人类可操作工具面 | VCPChat、Open WebUI、LobeHub、Cherry Studio | 工具表单、审批、工作流或专用控制面 | 已由 Agent 工具、Chat UI、生成运行时分别覆盖 | **不独立成类** |
| 模型评估与竞技 | Open WebUI | 评测话题、反馈、排名或比较结果 | 不与现有主题冲突，但只有一个样本 | **暂缓，补第二样本** |

## 已有类目复核

现有类目总体仍能覆盖当前项目，但有六处需要在后续维护中重新对齐。这里的“调整”主要指收紧定义、增加交叉引用或拆出子分型，不表示已有单项目结论失效。

| 现有类目 | 观察到的重叠或漂移 | 建议调整 | 紧迫度 |
| --- | --- | --- | --- |
| 对话导出与分享 | 指南的 E5 已纳入“为训练、评估或研究生产可移植会话轨迹”，与研究轨迹候选重叠 | 保留普通导出、分享和研究发布的交付语义；研究数据交付继续作为 E5 分型，不另建“研究轨迹”顶层类目 | 高 |
| 生成式输出与运行时 | 指南同时把“文件与项目工作区”列为相邻边界，部分单项目笔记又把 workspace、文档和 Artifact 放在同一层 | 将“模型生成的独立输出对象”与“围绕持久对象的协同编辑”分开：前者留在本类，后者转入协同工作区；在两类笔记中只保留交接点 | 高 |
| 对话请求与上下文 | 同篇同时承接完整生成任务和上下文规则编译；可配置的显示正则又与消息渲染器重叠 | 将规则对象、作用域、编译顺序和模型可见结果移入“上下文编译与提示词工程”；请求类目只保留编译发生在主链中的交接点 | 高 |
| 检索增强与认知编排 | 既覆盖知识/记忆资产检索，也记录记忆生命周期；记忆演化候选容易重复写入、整理和用户建模 | 本类只回答索引、查询、候选、重排和注入；记忆的形成、修订、过期、人工确认和用户画像另作边界专题，满足样本条件后再拆类 | 高 |
| 会话与消息管理、外部执行体与应用协作 | 前者承接跨设备同步和恢复，后者承接外部交互表面；多表面连续性候选横跨两边 | 以“同一逻辑会话/事件流是否跨表面保持稳定身份”为准入；普通数据库同步留在会话管理，外部 Agent/业务应用托管留在外部协作，多表面专题只比较身份、事件、接管和恢复 | 中 |
| 独特功能 | 自然聚类升级为正式类目后，部分旧候选仍保留边界研究链接，且当前工作区缺少这些文件 | 正式类目建立后，在待查清单中改链到实际横向对比或边界文档；缺失文件不得继续作为“已完成边界研究”的证据 | 中 |

### 研究数据与会话档案的分型

现有导出指南已经明确 E5“发布与研究”，因此不应再从导出类目复制一套“研究轨迹”格式矩阵。建议把候选能力按交付对象分型：对话导出记录从会话抽取什么、怎样脱敏和交付；离线轨迹生成记录批量运行、压缩和训练数据准备；会话档案记录导入、分享、同步和续作。Pi、Hermes Agent、OpenCode 目前分别落在这三种形态，不能视为同一类目。

### 生成式输出与协同工作区的交接

生成式输出类目已有 G4“可编辑工作区”与文件项目交接，说明它可以承接模型输出进入工作区的瞬间；协同工作区若正式建立，应接手对象创建之后的共同编辑、版本、锁、审批和跨参与者恢复。这样可避免把一次 Artifact 编辑器误计为协作，也避免在工作区笔记中重复输出协议和运行时隔离。

### RAG 与记忆演化的交接

RAG 横向对比已按谱系区分知识资产、工具检索、发送前选择和认知编排。后续若调查记忆演化，应把持久事实的写入触发、冲突修订、淘汰和用户模型影响作为主链；查询向量、候选集合和重排算法只引用 RAG 笔记。没有独立写回或整理生命周期的 World Info、固定摘要和一次性压缩，不应因为“记忆”字样迁入新类目。

### 多表面连续性的归属

当前会话管理和外部协作的边界已经足以承接大多数案例，但多表面候选仍有独立比较价值。建议先在 Chat 或外部协作横向文档增加“稳定会话身份、事件重放、所有权接管、取消与终态一致性”字段；只有这些字段在三个以上项目形成共同事实对象后，再拆为正式类目。

## 建议独立成类的方向

### 上下文编译与提示词工程

该方向的共同对象是可编辑、可持久化的上下文规则或模板，以及它们按作用域、角色、深度和阶段编译后的结果。AIO Hub、SillyTavern、Risuai、VCPChat 和 VCPToolBox 已提供足够样本：规则可能表现为正则、宏、模板、占位符或预处理器，但都能沿“配置 → 编译 → 模型请求或显示投影”追踪。分类依据是规则改变的消费面，不是实现是否使用正则。

建议把同一规则系统的作用点拆成三种分型：

1. **请求层编译**：改变用户输入、历史消息或最终 prompt，模型能看到变换结果；这部分从对话请求与上下文中抽出，作为本类主链。
2. **消息生命周期变换**：生成后清洗并写回权威消息，或改变会话消息的持久化内容；这里只在本类记录与请求/渲染的交接，具体落盘语义留在会话与消息管理或生成式运行时。
3. **显示层渲染**：只改变用户看到的文本、Markdown 或 DOM，原始消息和模型请求保持不变；`editdisplay`、`markdownOnly` 和前端 `applyFrontendRegexRules` 属于消息渲染器的用户可配置变换子节。

判断边界时，应优先检查变换结果是否进入模型、是否写回权威历史以及是否只存在于当前显示投影。一个规则同时拥有 prompt、output 和 display 作用点时，在本类统一描述规则对象和编译顺序，再分别链接请求、消息管理和渲染器的作用点，避免为“正则”重复建立三套类目。

### 主动 Agent 与后台任务

共同点不是“有定时器”，而是运行可以脱离当前用户回合，拥有独立触发、执行状态和结果交付。VCPChat 的 FlowLock/TopicSponsor、VCPToolBox 的 TaskAssistant、Hermes Agent 的 follow-up 与 cron、Open WebUI 的 Automations/Calendar、LobeHub 的 Schedule、AstrBot 的概率唤醒和后台任务均提供了候选样本。候选清单已将这些能力列为同一自然聚类，但尚未建立统一横向文档（见 [`独特功能/待查清单.md`](独特功能/待查清单.md)）。

建议正式类目按以下字段比较：触发者（用户、模型、系统或外部事件）、运行所在会话、调度粒度、并发和取消、结果归属、失败重试、空闲/忙碌时行为以及重启恢复。cron 隔离会话、heartbeat/flow 重新进入当前会话和 IM 主动插话应作为不同分型，不能只按“后台任务”合并结论。

边界上，普通一次性工具调用归入 Agent 工具；外部 CLI/业务应用的发现与托管归入外部执行体与应用协作；只有当任务拥有独立调度或非用户即时触发的生命周期时，才进入本类。

### 协同工作区

该方向的共同目标是让人和 Agent 围绕持续存在的文档、项目、文件或页面共同工作。VCPChat 的 VDOCX/VPPTX 与 Agent PR、LobeHub 的文档/项目和 Page-Agent、Open WebUI 的 Notes/Yjs、Cherry Studio 的 workspace、OpenCode 的文件/Git 工作区提供了不同分型。相关候选已在 README 和独特功能清单中出现，但目前没有统一类目。

比较时应先确定事实源（文件、文档数据库、消息或 Git）、编辑方式（全文、定向 patch、PR 或节点操作）、版本和冲突语义、锁与审批、Agent 可见范围、运行状态回流以及跨设备恢复。单纯的 Artifact 预览或消息内 Canvas 不足以进入本类，除非存在可持续对象和后续编辑链。

### 研究数据、轨迹生成与会话档案

Pi 的 `/export`、`/share` 和伴生 HF 发布工具面向会话数据交付；Hermes Agent 的 batch runner 与 trajectory compressor 面向离线批量生成和训练数据准备；OpenCode 的导出/导入、分享同步和 PR 续作面向可移植会话档案。三者的事实对象和主要消费者不同，不能仅因都保留“轨迹”就建立统一类目。

后续记录时，研究数据交付归入对话导出与分享的 E5 分型；轨迹压缩与批量生成作为 Agent 运行时或项目独特能力说明；会话档案的导入、分享、同步和续作仍按会话对象与交付语义记录。只有出现同一对象模型、同一消费者和可比较生命周期的第三个样本，才重新考虑独立分类。

## 需要先做边界专题的方向

### 记忆演化与用户建模

VCP 系、Hermes Agent、LobeHub、Open WebUI 和 SillyTavern 都有长期事实、摘要、用户画像或记忆写回，但事实源和循环所有者差异很大。检索增强横向对比已经覆盖摄取、候选、重排、写回和注入；若另建类目，必须把“如何找回内容”与“如何形成、修订和淘汰长期事实”分开。

建议准入条件是：存在跨回合或跨会话的持久记忆对象；有明确写入/整理触发；能描述事实修订、冲突、过期或人工确认；并且该对象会影响后续 Agent 或用户建模。仅有向量检索、固定摘要、World Info 注入或一次性上下文压缩时，继续归入检索增强或请求与上下文。

### 多表面与远程连续性

候选项目覆盖 Web、TUI、桌面、IM 和远程控制，但“多端”本身不是共同机制。正式比较应要求同一逻辑会话或事件流在两个以上表面间保持稳定身份，并至少具备同步、恢复、接管或取消中的一项。Hermes 的事件网关、OpenCode 的事件源与所有权接管、DeepChat 的 IM 控制、AstrBot 的平台投影、VCPChat 的移动端/桌面交接和 Open WebUI 的多表面能力可作为样本。

在完成边界研究前，跨设备数据库同步归会话管理，外部 Agent/业务应用托管归外部协作，平台消息序列化归 Chat 概览或消息渲染。该方向更适合先作为 Chat 或外部协作的扩展章节。

### Agent 社会与群体

当前候选混合了四种对象：多人群聊、父子 Agent 委派、可管理的 Agent Group，以及 Agent 论坛/社区。它们的拓扑、权限和结果聚合方式不同。建议先按“参与者身份、通信方向、上下文隔离、协调者、结果汇总、持久关系”做分型；至少有三种项目在同一分型上形成主链后，再决定是否建立独立类目。现阶段可在 Agent 角色、Agent 工具和外部协作横向文档中交叉引用。

## 暂缓或归并方向

### 模型评估与竞技

Open WebUI 的 Arena/ELO 已形成独立产品链，但现有项目中未找到第二个同质样本。继续保留为 Open WebUI 的独特能力；若后续加入 Chatbot Arena、并排评测或自动基准项目，再按评测对象、反馈采集、排名算法、话题采样和结果回流建立局部类目。

### 人类可操作工具面

VCPChat 的 HumanToolBox、Open WebUI 的工具/工作流面、LobeHub 的控制面和 Cherry Studio 的工作区操作都体现“人可以接管 Agent 能力”，但它们的事实对象和执行域不同。该方向更适合作为 Agent 工具与 Chat UI 的共同比较字段：入口是否由模型生成、是否有审批、是否可重复、结果是否回到会话或工作区。独立成类会重复已有调查且难以形成同质样本。

## 推荐实施顺序

1. **上下文编译与提示词工程**：准入条件已满足，先建立独立横向文档，并把请求层、消息生命周期和显示层作用点分开比较。
2. **协同工作区**：对象差异较大，建议先做准入和事实源边界，再建立正式类目。
3. **记忆演化与用户建模**：先补边界专题，避免与现有 RAG 结果重复；随后根据是否能形成共同生命周期决定立项。
4. **多表面与远程连续性、Agent 社会与群体**：先做分型和局部证据盘点，不急于建立顶层目录。
5. **模型评估与竞技**：等待第二个同质项目。

## 未验证事项

- 本调查基于已有笔记和横向文档，没有重新运行各项目；候选方向的运行时、平台差异、性能和恢复行为仍以来源笔记的未验证事项为准。
- “建议独立成类”表示分类条件和样本覆盖已接近满足，不表示所有候选项目都拥有等价能力，也不表示各项目的实现质量相同。
- 上下文编译与提示词工程本次已完成分类边界判断，但尚未建立独立横向对比文档；各项目规则的运行命中、编辑器行为和多作用点交互仍以来源笔记的未验证事项为准。
- README 和独特功能清单引用了若干边界研究名称，但当前工作区未发现对应文件；本报告未把这些缺失引用当作已完成的边界证据，判断仅基于现存横向文档和单项目笔记。

## 关键来源索引

- [独特功能待查清单](独特功能/待查清单.md)：候选聚类、项目状态和升级记录。
- [检索增强与认知编排横向对比](检索增强与认知编排/检索增强与认知编排横向对比.md)：记忆、知识资产和检索循环的既有边界。
- [外部执行体与应用协作横向对比](外部执行体与应用协作/外部执行体与应用协作横向对比.md)：外部 Agent、业务应用和控制表面的既有边界。
- [会话与消息管理横向对比](会话与消息管理/会话与消息管理横向对比.md)：会话事实源、同步和恢复边界。
- [Agent 工具横向对比](Agent工具/Agent工具横向对比.md)：工具注册、审批、执行和回注边界。
- [生成式输出与运行时横向对比](生成式输出与运行时/生成式输出与运行时横向对比.md)：Artifact、文件工作区和运行对象边界。
- [对话请求与上下文横向对比](对话请求与上下文/对话请求与上下文横向对比.md)：请求组装、上下文注入和执行链边界。
- [消息渲染器横向对比](消息渲染器/消息渲染器横向对比.md)：显示投影、流式重绘和消息内容变换的交接。
- [主动 Agent 与后台任务横向对比](主动Agent与后台任务/主动Agent与后台任务横向对比.md)：已落地的触发、运行状态、执行、交付与恢复比较。
- [AI 客户端特色功能贡献统计](AI客户端特色功能贡献统计.md)：特色能力族和机制贡献族的现有归并口径。
