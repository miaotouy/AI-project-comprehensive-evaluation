# 应用界面基础设施横向对比

> 对比对象：AIO Hub、AstrBot、Chatbox、Cherry Studio、DeepChat、DeepSeek Harness、Dify、Hermes Agent、Jan、LobeHub、Manifold Desktop、NextChat、Open WebUI、OpenCode、Risuai、SillyTavern、VCPChat、VCPToolBox；Pi 作为 TUI 形态单列
>
> 对比更新日期：2026-08-28
>
> 依据：本类目 19 份单项目调查笔记及其记录的代码快照
>
> 对比方法：按应用装配、弹窗与浮层、通知、主题、响应式、内容交互和验证范围统一比较；表格只保留共同维度，项目细节回到单项目笔记
>
> 对比范围：应用级界面基础设施；聊天业务主链和消息内容渲染分别由 Chat UI 与消息渲染器类目承接
>
> 文档定位：实现学习与跨项目横向比较，不作为整改方案

## 结论摘要

这些项目没有形成共同的界面基础设施方案。弹窗既有 Radix、Reka、Kobalte、Vuetify 和 Mantine 等依赖，也有原生 dialog、手写 DOM 和独立窗口；通知既可能是组件库 Toast，也可能包含持久通知中心、跨窗口语义路由或终端反馈协议。

成熟度差异主要体现在“公共机制覆盖到哪里”。Cherry Studio、DeepChat、Hermes Agent 和 LobeHub 已把弹窗、主题或通知收束到公共层；AIO Hub 和 VCPToolBox 也有完整自研基座。AstrBot、SillyTavern 等项目则更多由业务页面直接消费库或局部实现，统一入口较少。

主题是差异最大的维度。部分项目只提供明暗两档，部分项目支持主色、字体和背景图，Hermes Agent、SillyTavern、AIO Hub 与 VCPToolBox 已形成主题资源或外观编辑子系统；其中 AIO Hub、Cherry Studio、SillyTavern 与 VCPToolBox 还提供用户自定义 CSS。主题权威源也分散在 localStorage、服务端设置、主进程设置文件和终端环境中。

Risuai 把主题权威源放在加密存档的数据库对象里，配色预设、文字主题、字体和自定义 CSS 都与业务数据同库持久化，且全仓库没有 prefers-color-scheme，完全不跟随系统明暗。命令式反馈收敛为单一 alertStore：模态、确认、等待、进度与 toast 共用一条路径、一次只显示一个；业务模态则是各自手写的 fixed inset-0 遮罩，与命令式路径互不共享底层。Risuai 没有图片灯箱。

静态源码能确认装配、状态流和事件绑定，却不能替代对焦点顺序、Toast 堆叠、触摸手势、多窗口同步和终端协议的运行验证。下表中的“依赖默认”均表示项目侧没有覆盖，不能直接推定实际行为已经验证。

VCPChat 当前把聊天 surface、任务与 contribution registry 的可释放资源分别收束到生命周期控制器，且受管安装器与 Electron 主窗口不共享 UI 运行时；AstrBot 的配置表单新增 secret 字段掩码与平台编辑器加载状态。它们扩大了公共层覆盖，但尚不足以由静态入口推断焦点、升级或跨窗口体验。

## 1. 产品表面与公共层

| 项目 | 产品表面 | 公共界面层 | 主要状态所有者 |
|---|---|---|---|
| AIO Hub | Tauri 桌面 | Vue 共享组件与 composable | Pinia、设置文件、跨窗口总线 |
| AstrBot | Web 管理台 | Vuetify 直接消费，少量公共插件 | Pinia 与 localStorage |
| Chatbox | Web、Electron、移动端 | Mantine、MUI、vaul、Radix 并存 | 设置存储、zustand、jotai |
| Cherry Studio | Electron 多窗口 | 内部 UI 包与 Popup Host | 主进程设置、模块级 store |
| DeepChat | Electron 多窗口 | shadcn-vue、Reka、dc-ui | 主进程设置与渲染层 store |
| DeepSeek Harness | Web（浏览器 SPA + node:http 宿主） | 自研 ui-primitives + slot 插槽系统 | 对象层（会话/连接）+ 声明 store + 宿主设置文档 |
| Dify | Next Web（Console 与公开应用页） | `@langgenius/dify-ui` + 业务组件 | 根 Provider、Console context、Jotai 与 Query 状态 |
| Hermes Agent | Electron、TUI、Web | 三套独立组件层，共享后端协议 | 各表面独立 store |
| Jan | Tauri、Web、移动端 | shadcn 风格 Radix 组件 | zustand 与后端设置 |
| LobeHub | Web、桌面、移动端 | `@lobehub/ui` 与 antd-style | next-themes、用户 store |
| Manifold Desktop | WinUI + WebView2 | 原生 DOM/CSS 单例组件 | 原生设置与局部 JS 状态 |
| NextChat | Web、PWA、Tauri | 自研 React UI 库 | zustand 配置与局部 root |
| Open WebUI | SvelteKit SPA | 自研 common 组件 | Svelte store、服务端设置 |
| OpenCode | TUI、Web、Electron | opentui 与 Kobalte 两套 | 各表面独立 store |
| Risuai | Tauri 桌面、Web | 无现成组件库，自研 GUI 基础件 | DBState 内存响应式 + 各类 store |
| SillyTavern | Web | 原生 Web、自研 Popup、jQuery 生态 | 服务端设置与全局状态 |
| VCPChat | Electron 多窗口 | 手写 DOM 与 template | 设置文件与渲染层状态 |
| VCPToolBox | Vue 管理台 | 自研组件与 feedback Host | Pinia、localStorage、模块状态 |

共同趋势是应用公共层通常只覆盖“怎么显示”，业务 store 仍决定“何时显示”。DeepChat 的通知和 Hermes Agent 的贡献注册表是少数把策略或应用装配也纳入基础设施的例子。

## 2. 弹窗、浮层与焦点

| 项目 | 主弹窗机制 | 关闭策略 | 焦点边界 |
|---|---|---|---|
| AIO Hub | 自研 BaseDialog | Esc 与关闭按钮共用开关；遮罩可配置 | 项目侧没有统一焦点陷阱 |
| AstrBot | Vuetify Dialog | 依赖默认；persistent 可阻止关闭 | 依赖内部未核实 |
| Chatbox | Mantine 为主，vaul 负责移动端 | 逐弹窗配置 | 部分场景主动关闭焦点陷阱 |
| Cherry Studio | 内部 UI 包封装 Radix | 支持延迟卸载和业务覆盖 | Radix 管理，支持指定归还目标 |
| DeepChat | Reka Dialog 与全局消息框 | 依赖默认；自研 Spotlight 单独处理 | Reka 未下钻；Spotlight 无陷阱 |
| DeepSeek Harness | 受控 Modal（portal 到 body） | Esc 与遮罩关闭 | Modal 无焦点逻辑；lightbox 归还焦点，均未运行验证 |
| Dify | Dify UI overlay + Console ModalContext | 业务 Modal 由各组件控制 | Dify UI 内部未下钻；项目侧未运行验证 |
| Hermes Agent | Radix Dialog | 依赖默认；Portal 容器处理嵌套浮层 | Radix 未下钻 |
| Jan | Radix、vaul 与全局门控 | 依赖默认 | Radix 未下钻 |
| LobeHub | base-ui 命令式弹窗 | 多数允许遮罩关闭，高危场景关闭 | base-ui 未下钻 |
| Manifold Desktop | 手写 DOM 单例 | 各组件自行处理 | Confirm 缺 Esc、陷阱与归还 |
| NextChat | 每次调用新建 React root | 遮罩点击关闭，无退场阶段 | 无统一焦点管理 |
| Open WebUI | 自研 Modal + focus-trap | Esc、遮罩与滚动锁定 | 项目侧已接入 focus-trap |
| OpenCode | TUI store 栈；Web Kobalte 栈 | TUI 与 Web 各自统一 | TUI 显式归还；Kobalte 未下钻 |
| Risuai | 无共享基座；业务模态手写 fixed inset-0 遮罩，命令式交互走单一 alertStore | alert 主模态仅 Esc/按钮；业务模态遮罩点击关闭 | 无统一焦点陷阱；唯一焦点动作静态推断为空引用 |
| SillyTavern | 原生 dialog + Popup | 遮罩不关闭；阻塞弹窗双击 Esc | 有归还，没有焦点陷阱 |
| VCPChat | template 弹窗与 Promise 确认框 | 确认框支持 Esc、Enter、遮罩 | 无焦点陷阱 |
| VCPToolBox | BaseModal + feedback Host | BaseModal 完整；命令式确认框较弱 | BaseModal 有陷阱，其他弹窗不一致 |

弹窗“使用同一底层”并不等于行为一致。Chatbox、VCPToolBox 和 DeepChat 都存在不同弹窗类型采用不同焦点或关闭规则的情况。依赖库项目还需要区分项目侧配置和依赖默认，静态调查不能把后者写成已验证事实。

## 3. 通知、Loading 与错误恢复

| 项目 | 临时反馈 | 持久或跨窗口反馈 | 错误恢复层 |
|---|---|---|---|
| AIO Hub | Element Plus 包装与严重级分发 | 通知中心 | Vue 全局钩子、Rust 探针、启动兜底 |
| AstrBot | 单例 Snackbar 队列 | 无独立中心 | axios 拦截与业务捕获 |
| Chatbox | MUI Snackbar + sonner | 无统一中心 | 多层 Sentry ErrorBoundary |
| Cherry Studio | 自研 Toast store | 每窗口 Host | 窗口、路由与消息多层边界 |
| DeepChat | NotificationManager + sonner | 主进程跨窗口语义路由 | 功能级反馈为主，无应用总边界 |
| DeepSeek Harness | 受控 Toast（无队列） | 无持久通知中心 | 插槽条目级 SlotErrorBoundary + HTTP 每请求兜底 |
| Dify | 根部 ToastHost（5 秒、最多 3 条） | 未确认独立持久中心 | 业务边界与运行恢复未逐项调查 |
| Hermes Agent | 双栈通知 | 后端通知协议 | 根边界与贡献 slot 边界 |
| Jan | sonner | 无系统通知 | 根级对话框和业务捕获 |
| LobeHub | base-ui Toast | 独立悬浮通知卡 | 路由、局部、启动与 chunk 分层 |
| Manifold Desktop | 单条手写 Toast | 无 | 前端局部处理 |
| NextChat | 手写 Toast | 更新系统通知 | 前端错误提示为主 |
| Open WebUI | svelte-sonner | 浏览器通知与 webhook | 业务级错误反馈 |
| OpenCode | TUI 单条；Web 两代 Toast | 浏览器系统通知 | TUI 与 Web 各自处理 |
| Risuai | alertStore 单条 toast，不堆叠、无队列 | 浏览器系统通知（Web Notification）；单窗口 | window error 与 unhandledrejection 汇入错误模态，vite:preloadError 兜底 |
| SillyTavern | toastr | Action Loader | 有限重载，无全局错误边界 |
| VCPChat | 浮动 Toast | 持久通知侧栏 | 自定义错误提示 |
| VCPToolBox | feedback Host | WebSocket 通知列表 | 业务捕获，无应用错误边界 |

Loading 也呈现两种路线。LobeHub 和 Cherry Studio 有共享骨架组件，AIO Hub 会按真实布局绘制聊天骨架；Chatbox、Manifold Desktop 等项目则更多使用文本、局部 spinner 或场景自建占位。

错误恢复最完整的不是 Toast 数量最多的项目，而是边界层次清楚的项目。LobeHub 区分路由、局部组件、启动和分包加载；Cherry Studio 区分窗口、路由和消息；Hermes Agent 还会把插件故障限制在贡献 slot 内。

## 4. 主题权威源与可配置面

| 项目 | 权威源 | 系统跟随 | 用户可配置面 |
|---|---|---|---|
| AIO Hub | 设置文件 | 支持 | 明暗、主色、壁纸、混合与窗口效果、自定义 CSS（含预设） |
| AstrBot | localStorage + Vuetify theme | 支持 | 明暗、主色、次色 |
| Chatbox | 设置存储与首屏缓存 | 支持 | 明暗、品牌色、字号、两级背景图 |
| Cherry Studio | Electron 主进程 | 支持 | 明暗、主色、字体、自定义 CSS |
| DeepChat | 主进程设置 | 支持 | 明暗、字号、正文与代码字体 |
| DeepSeek Harness | 宿主设置文档 + index 内联脚本 | 支持（system 经 prefers-color-scheme） | 明暗/system 三值 + --dsw-* token 叠层 |
| Dify | next-themes；HTML `data-theme` | 支持 | system/light/dark；持久化介质未下钻 |
| Hermes Agent | 三端各自持有 | 支持 | 主题市场、主题资源、排版与布局 |
| Jan | zustand + 后端设置 | 支持 | 明暗、字号、强调色 |
| LobeHub | next-themes + 用户 store | 支持 | 明暗、预设色、动画、聊天外观 |
| Manifold Desktop | 原生设置文件 | 不支持 | 明暗、字号 |
| NextChat | zustand + IndexedDB | 支持 | 明暗、字号、字体 |
| Open WebUI | localStorage；背景图走账户设置 | 支持 | 明暗、OLED、聊天背景图 |
| OpenCode | TUI KV；Web localStorage | 支持 | 主题选择与库级注册 API |
| Risuai | 加密存档中的数据库对象 | 不支持（无 prefers-color-scheme） | 17 套配色预设 + 自定义配色、文字主题、字体、自定义 CSS、布局主题 |
| SillyTavern | 服务端主题 JSON | 不支持 | 色板、背景图库、字号和多项 UI 偏好 |
| VCPChat | 设置文件 + CSS 文件 | 支持 | 主题选择；桌面壁纸另成系统 |
| VCPToolBox | localStorage | 不支持 | 预设、颜色、自定义 CSS、背景图与布局档位 |

### 主题能力分组

**基础明暗层。** Manifold Desktop、NextChat、Jan 和 DeepChat 的主题入口较收敛，重点是明暗、字号或字体。它们更容易解释权威源，但自定义能力有限。

**产品外观层。** AIO Hub、Chatbox、Open WebUI 和 VCPToolBox 把背景图、主色或布局偏好纳入主题。AIO Hub 还把自定义 CSS 做成独立设置子系统，提供内置与用户预设、编辑器实时注入和设置文件持久化；主窗口和分离窗口初始化都会调用持久化恢复入口，不再依赖先打开设置页。这里也容易出现权威源分裂，例如 Open WebUI 的明暗模式留在设备本地，背景图却随账户同步。

**主题资源层。** Hermes Agent、OpenCode、Pi 和 SillyTavern 支持主题文件、资源注册或扩展贡献。Hermes Agent 还包含 VS Code Marketplace 查询、VSIX 下载、转换和用户注册表；Pi 通过文件系统、包与扩展发现主题，但没有市场 UI。

**特殊切换方式。** VCPChat 通过覆写 CSS 文件后重载窗口切换主题，代价与 CSS 变量热替换不同。LobeHub 则把明暗模式和主色分别交给 next-themes 与用户 store，首屏还需 cookie 和 HTML 脚本参与。

## 5. 响应式、移动端与窗口

| 项目 | 主要适配方式 | 移动端或窄屏形态 | 多窗口同步 |
|---|---|---|---|
| AIO Hub | 媒体查询、自研尺寸调整 | 桌面工具为主 | 业务总线与独立设置通道 |
| AstrBot | Vuetify 断点与局部判断 | 管理台窄屏 | 主题主要靠浏览器状态 |
| Chatbox | 约 600px 断点与 AdaptiveModal | Drawer 与移动页面 | 设置驱动多表面 |
| Cherry Studio | 响应式组件与 Electron 窗口 | 多窗口桌面 | 主进程广播主题 |
| DeepChat | 手动侧栏与窗口入口 | 独立浮窗、设置窗 | IPC 快照与 revision |
| DeepSeek Harness | 三列让步解算 + 1024px 自动折叠 | 同一 SPA，无独立移动端入口 | 单窗口（无多窗口概念） |
| Dify | viewport + 业务路由/CSS 断点 | 未逐路由核实 | 单浏览器上下文；Console/公开页 provider 不同 |
| Hermes Agent | pane 树与窄屏 overlay | 桌面、TUI、Web 独立 | storage 与后端协议 |
| Jan | 768px Sidebar Sheet | 同一 bundle | Tauri 主题广播 |
| LobeHub | 独立移动路由与构建 | 底部 TabBar | Web 状态与原生明暗同步 |
| Manifold Desktop | 固定 WebView 布局 | 无移动端 | 单窗口为主 |
| NextChat | 600px 单断点 | CSS 抽屉 | 配置持久化 |
| Open WebUI | 768px store 分支 | 同一 SPA | localStorage 与服务端设置 |
| OpenCode | Web 断点；TUI 尺寸布局 | Web、TUI 独立 | 各表面独立 |
| Risuai | DynamicGUI（≤1024px）与 MobileGUI（≤800px）两级 + Tailwind 断点 | 移动端独立三屏骨架 | 单窗口（Tauri 单实例），无跨窗口同步需求 |
| SillyTavern | 媒体查询与触摸规则 | 抽屉铺满视口 | 服务端设置 |
| VCPChat | 设置控制 Compact 模式 | 非自动断点切换 | Electron IPC |
| VCPToolBox | 媒体查询与容器查询 | 移动侧栏抽屉 | storage 事件 |

LobeHub 的移动端边界最明确：它是独立入口和独立产物。Jan、NextChat 和 Open WebUI 则在同一前端中根据断点改变侧栏与路由周边。VCPChat 的 Compact 导航属于用户偏好，不能写成响应式行为。

## 6. 图片、拖放与剪贴板

| 项目 | 图片查看 | 拖放基础设施 | 剪贴板特点 |
|---|---|---|---|
| AIO Hub | viewerjs 灯箱 | 文件拖入与尺寸调整 composable | Tauri 与 Web 能力结合 |
| AstrBot | 多处复制的 overlay | 页面分别实现 | 浏览器接口 |
| Chatbox | 缩放和平移灯箱 | react-dropzone 与 dnd-kit 分工 | 双通道工具与反馈 hook |
| Cherry Studio | 完整图片预览器 | 多处业务拖放 | 支持变换后导出 |
| DeepChat | 全屏查看与保存 | 场景实现 | Electron 桥接 |
| DeepSeek Harness | ImageLightbox 灯箱（ui-attachment） | 业务级拖放（InputBar 图片拖入、workspace 行重排） | 浏览器 API |
| Dify | 音频/视频图库、语音输入、图片/文件 feature 组件 | 浏览器媒体与文件 API；完整预览链未确认 | 业务组件与页面状态 |
| Hermes Agent | 轻量灯箱；文件预览走 rail | 公共 drop affordance | 统一工具层 |
| Jan | 未接线的 ImageModal | 场景实现 | 组件直接调用浏览器 API |
| LobeHub | PreviewGroup 灯箱 | 资源管理器原生拖放 | Web 与桌面能力 |
| Manifold Desktop | 未实现 | 附件通道未接通 | 原生桥提供复制 |
| NextChat | 消息内普通图片 | 上传通道依赖 Service Worker | 浏览器 API |
| Open WebUI | 缩放、平移与下载 | 上传区与业务拖放 | 浏览器 API |
| OpenCode | Web 图片预览；TUI 协议图像 | TUI 无鼠标文件拖放 | 浏览器与终端通道 |
| Risuai | 无灯箱，未找到预览组件 | 根元素文件导入分流 + 6 个自定义 MIME 阻断内部拖拽 | 无公共服务，直接调浏览器 API |
| SillyTavern | Popup 灯箱与放大镜 | 多个 Sortable 场景 | 浏览器 API |
| VCPChat | 独立窗口，含绘图与 OCR | 业务实现 | 原生剪贴板 |
| VCPToolBox | 仅局部预览 | HTML5 与指针拖动并存 | 浏览器 API |

图片能力最强的不一定是公共灯箱最完善的项目。VCPChat 通过独立窗口获得 OCR 和绘图，但运行与窗口管理成本更高；Cherry Studio 则把旋转、翻转、多图导航和保存留在统一预览组件中。

拖放方面，LobeHub 明确为大型虚拟列表选择原生 HTML5 实现；AIO Hub 和 VCPToolBox 提供部分公共会话或指针层；更多项目仍由每个业务页面分别处理拖入、排序和视觉反馈。

## 7. 动画与 reduced motion

多数项目没有为消息卡片首次出现增加明显动画。常见方案是组件库的开合过渡、CSS transition 和少量局部 motion，而不是全应用统一的页面动效体系。

- AIO Hub、NextChat、Manifold Desktop 和 VCPChat 以 CSS 过渡为主。
- Cherry Studio、DeepChat、Jan 和 OpenCode Web 使用 Tailwind 动画类与 Radix、Reka 或 Kobalte 状态属性。
- LobeHub 与 Hermes Agent 只在少数组件使用 motion；消息列表本身没有统一入场动画。
- SillyTavern 另有流式文本渐显，Pi 和 OpenCode TUI 使用字符帧、flash 或 spinner。
- VCPToolBox 的用户动画开关和 reduced-motion 覆盖最广；DeepChat 也有全局动效 token 与 reduced-motion 兜底。
- Risuai 以零散 CSS 动画为主：全局时长变量、toast 淡入淡出与存档保存动画；未发现 prefers-reduced-motion 处理。

项目内自定义动画不一定服从全局开关。LobeHub 的面板滑动会检查动画设置，但 WorkflowCollapse 没有同类判断；这类差异需要逐组件核对，不能只凭依赖清单判断。

## 8. 代表性设计差异

**公共层迁移。** Chatbox、LobeHub 和 OpenCode Web 都保留两代基础设施。兼容层能降低迁移成本，也使同类反馈的行为在一段时期内不完全一致。

**跨窗口通知。** DeepChat 将通知拆成渲染层仲裁和主进程语义路由，能按窗口能力与焦点状态投递。其它 Electron 项目多以每窗口 Host 或渲染层浏览器通知为主。

**插件与贡献隔离。** Hermes Agent 的应用壳由贡献注册表组织，插件渲染失败会限制在 slot 内；DeepSeek Harness 的整个 UI 就是 Cordis 插件组合，每个插槽条目包 SlotErrorBoundary，装配错误则重新抛出。相比简单的根 ErrorBoundary，这种边界更贴近可扩展应用的所有权结构。

**无框架自研。** Manifold Desktop、NextChat、VCPChat 和 VCPToolBox 都有手写基础设施，但完整度差异很大。VCPToolBox 的 BaseModal 和 feedback Host 已形成公共机制；Manifold Desktop 和 NextChat 仍以模块单例或每次创建 root 为主。

**主题与外观边界。** SillyTavern 的主题文件会打包大量界面偏好，AIO Hub 把外观效果和自定义 CSS 分成两套独立系统，Open WebUI 将背景图放在账户设置，Hermes Agent 则把主题当可安装资源。它们都叫“主题”或“外观”，实际数据边界并不相同。

**单一模态与存档主题。** Risuai 的命令式反馈收敛为单一 alertStore，模态、确认、等待、进度与 toast 一次只显示一个、不可堆叠；业务模态各自手写 fixed 遮罩，与命令式路径互不共享。它的主题权威源落在加密存档数据库对象里、与业务数据同库持久化，不跟随系统明暗，也没有图片灯箱。

## 9. Pi：TUI 形态映射

Pi 是纯 TUI 项目，不适合直接放进 GUI 弹窗、灯箱和断点表格。对应关系如下：

| GUI 概念 | Pi 中的对应机制 |
|---|---|
| Modal / Overlay | overlay 栈；常规选择器通过编辑器插槽替换 |
| Esc 与焦点 | 统一键位注册表；overlay 关闭后恢复 preFocus |
| Toast | 聊天区状态文本、fullscreen flash、OSC 9;4 进度 |
| 主题 | JSON token、终端色彩检测、文件与扩展资源发现 |
| 响应式 | 根据终端行列重新布局 regular 或 fullscreen |
| 图片 | Kitty / iTerm2 协议，无能力时文本 fallback |
| 拖放 | 终端没有文件拖放事件，以粘贴路径和附件补全替代 |
| 剪贴板 | 原生插件、平台命令和 OSC 52 多层回退 |

Pi 的主题、键盘和终端协议基础设施很完整，但读屏、触摸和桌面窗口语义不适用于这一表面。所有终端行为仍需在具体模拟器中验证。

## 10. 未验证事项

- Radix、Reka、Kobalte、Vuetify、Mantine、base-ui 和 focus-trap 等依赖的默认焦点、Esc、滚动锁与动画行为，除单项目笔记注明外均未下钻。
- Toast 的像素级堆叠、遮挡和标题栏避让没有进行统一截图测试。
- 主题首屏、多窗口同步、系统跟随和自定义字体加载多为静态链路确认，尚未跨平台重放。
- AIO Hub 自定义 CSS 的设置、持久化、当前文档注入和主/分离窗口启动恢复链已静态确认；真实重启、多窗口时序与样式视觉结果尚未运行验证。
- 图片灯箱的触摸手势、超大图片性能、OCR 效果和变换后导出质量未统一实测。
- 响应式结论主要来自断点、路由和状态分支；实际窗口拖动、软键盘和安全区域表现未逐项目验证。
- 无障碍结论来自静态语义与事件绑定，不代表完成 WCAG 或读屏审计。
- Pi 的 Kitty、iTerm2、OSC、IME 和 fullscreen 行为未在不同终端模拟器中运行验证。

## 单项目笔记

详细证据、项目特例和源码入口见本目录各项目的“应用界面基础设施调查笔记”。横向表中的简写不能替代单项目笔记记录的调查边界。
