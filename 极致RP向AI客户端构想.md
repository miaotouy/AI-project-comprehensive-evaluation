# 极致 RP 向 AI 客户端构想：世界剧场

> 构想日期：2026-08-24
>
> 素材范围：本目录已有的角色、上下文、会话、渲染、工具、媒体与独特功能调查笔记
>
> 文档定位：跨项目机制拼装形成的产品构想，不代表任一被调查项目的现状，也不作为实现承诺

## 一句话定义

“世界剧场”是一套以**可分叉的世界事实**为核心、由幕后导演调度多个有限认知角色、再把结果投影为文本、立绘、声音与场景的本地优先叙事运行时。

它追求的“极致 RP”包含五个可检验结果：

1. 角色长期保持自己的欲望、语气、关系与知识边界。
2. 剧情发生过的事会留下后果，物品、承诺、伤势、秘密和关系不会随上下文裁剪消失。
3. 群像角色只依据自己看见、听见和被告知的内容行动，不共享一份全知提示词。
4. 用户可以改写、回退和探索平行可能，同时清楚知道哪条时间线是当前事实。
5. 后台编排、检索、校验和媒体生成不会穿帮成一串 Agent 工作日志。

## 核心判断

现有 RP 客户端大多把聊天记录同时当作剧情、世界状态和模型记忆。短对话时足够自然，长篇后会逐渐出现角色串台、秘密泄漏、设定漂移和摘要失真。极致方案需要把这几类事实拆开：

| 层次 | 权威对象 | 负责回答的问题 |
|---|---|---|
| 世界事实 | 追加式事件日志 + 当前状态投影 | 客观发生了什么，现在是什么状态 |
| 角色认知 | 每个角色独立的所见、所信、所误解 | 这个角色知道什么，认为发生了什么 |
| 叙事表达 | 一次表演的台词、动作、旁白与节奏 | 这一幕怎样讲出来 |
| 舞台投影 | 文本、立绘、语音、背景、音乐与交互部件 | 用户此刻怎样感知这一幕 |
| 创作控制 | 分支、重写、导演手势、边界与节奏设置 | 用户怎样介入而不打断表演 |

模型只能**提议**事件和状态变化，不能直接改写世界数据库。确定性规则、权限检查和连续性校验通过后，变化才提交到当前时间线。这样可以保留模型的表演自由，同时让事实有明确所有者。

## 六类一等对象

### 1. 世界包

世界包是可移植内容资产，包含基础设定、地点图、时间规则、阵营、物品类型、世界书、可选规则插件和初始事件。它可以引用多个角色包，但不拥有具体会话历史。

世界书沿用关键词、正则、递归、概率、扫描深度、sticky/cooldown/delay、包含组与位置注入等成熟语义；编辑器同时提供“本轮为什么命中”和“最终进入了哪个角色的认知”两种解释面。机制素材来自 [SillyTavern 角色与上下文调查](./Agent角色/SillyTavern-Agent角色配置调查笔记.md) 和 [AIO Hub 对话请求与上下文调查](./对话请求与上下文/AIO-Hub-对话请求与上下文调查笔记.md)。

### 2. 角色包

角色包兼容 Character Card V2/V3、PNG 与 CharX，继续承载人格、场景、示例对话、开场白和嵌入式世界书，并新增：

- 稳定身份与版本号；
- 私有认知库和初始错误信念；
- 欲望、禁忌、短期意图和长期弧线；
- 与其他角色的双向关系种子；
- 情绪立绘、声音、动作和称呼资源；
- 允许调用的“剧情动作”，例如观察、说服、检定和使用物品；
- 内容边界及淡出规则。

模型、采样参数和 Provider 保持为运行配置，避免角色卡被某个模型锁死。角色包中的内容事实与运行策略分离，这一点继承 SillyTavern 的可移植角色卡边界；编辑体验则采用 AIO Hub 的一体化消息配方、变量、资产和工具策略工作台。两者差异见 [Agent 角色横向对比](./Agent角色/Agent角色横向对比.md)。

### 3. 剧本战役

战役绑定世界包、参演角色、玩家档案和当前时间线。它保存一组场景、未决线索、关系网、时钟、任务和导演策略。角色包后续升级时，战役明确选择继续使用快照、迁移到新版或局部合并，禁止悄悄改变旧剧情。

### 4. 场景

场景是一次可管理的时空容器：地点、时间、在场者、可见物、氛围、当前冲突和退出条件都属于场景。只有在场且感知条件满足的角色，才会获得某个事件的认知副本。

### 5. 事件

事件是最小世界事实，例如“用户把钥匙交给伊芙”“守卫听见门后响声”“午夜钟推进一格”。事件至少记录：

```text
event_id / timeline_id / scene_id / logical_time
actor / action / targets / location
preconditions / accepted_state_delta
witnesses / perception_results
source_turn / provenance / confidence
```

台词也可以产生事件，但“角色说了某事”和“某事为真”是两条不同记录。谎言、误会和秘密由此可以长期存在。

### 6. 时间线

当前时间线是一条事件链及其状态投影。重写尚未提交的候选回复，只替换同一语义位置的候选；回到已经发生后续事件的旧节点，则创建新时间线。任何压缩、总结和迁移都保留源事件寻址关系。

这里组合了 SillyTavern 的 swipe、Cherry Studio/AIO Hub 的持久消息树，以及 Hermes Agent/Pi 的 fork 与 lineage 思路；现有能力分布见 [特色功能贡献统计](./AI客户端特色功能贡献统计.md#会话演化连续性与轨迹交付)。

## 一回合怎样运行

```text
用户输入
  -> 输入解释：台词 / 动作 / 心声 / OOC 指令
  -> 场景感知：计算每个角色本轮可见、可闻、可推断的信息
  -> 导演规划：选择出场者、冲突节拍和需要解决的悬念
  -> 规则裁决：检查物品、位置、时钟、检定和硬性世界约束
  -> 角色表演：各角色仅凭自己的认知生成意图、台词、动作和情绪
  -> 连续性审校：发现串台、越权知识、状态矛盾和重复节拍
  -> 叙事合成：把获准结果编成用户可见的一幕
  -> 舞台投影：流式正文 + 立绘/语音/背景/音乐提示
  -> 原子提交：事件、状态、角色认知、关系变化和消息版本一并落盘
  -> 异步整理：记忆索引、摘要、素材预取和下一场景候选
```

这条链里有三个关键约束：

1. **导演不替角色说话。** 导演只决定谁应回应、场景压力和叙事预算，角色模型负责具体表达。
2. **角色不读取世界全量真相。** 每名角色拿到独立编译的认知上下文，秘密只能经目击、告知、调查或推断传播。
3. **叙事文本不反向充当状态解析协议。** 角色同时返回结构化提议和自然语言表演；状态提交读取前者，消息渲染读取后者。

## 幕后 Agent 编制

“多 Agent”属于内部实现，不在聊天记录里排队自报家门。

| 幕后职责 | 何时运行 | 建议模型档位 |
|---|---|---|
| 导演 | 每轮；群像时决定发言者和节拍 | 强推理、中低温度 |
| 角色演员 | 轮到该角色时；可并发出草案 | 角色可独立绑定模型与采样 |
| 规则裁判 | 出现检定、资源或位置变化时 | 规则引擎优先，小模型补充解释 |
| 连续性编辑 | 有状态变化、多角色秘密或长跨度回归时 | 小模型初筛，强模型处理冲突 |
| 记忆策展 | 回合结束、场景结束和空闲时 | 嵌入 + 低成本模型 |
| 媒体调度 | 情绪变化、转场或用户点播时 | 分类器/嵌入优先，生成模型异步 |

不是每轮都调用全部模型。导演先给出执行图，确定性门控按场景复杂度启动所需节点；简单的一对一闲聊可以退化为一次角色生成加一次轻量提交。

VCPToolBox 的跨 Agent 长期通信和多观点会议证明了“独立身份 + 持久关系 + 有界回合”可以成为后台对象；本构想只借它的编排思想，不把三个固定评审人格直接搬到 RP 前台。相关素材见 [VCPToolBox 独特功能调查](./独特功能/VCPToolBox-独特功能调查笔记.md)。

## 记忆：事实、记忆和信念分开

每个角色拥有四层记忆：

| 层次 | 内容 | 维护方式 |
|---|---|---|
| 工作记忆 | 当前场景和最近若干轮 | 随场景窗口更新 |
| 情景记忆 | 角色亲历或被告知的事件 | 从见证结果生成，保留来源与时间 |
| 语义记忆 | 稳定人物知识、地点知识和习惯 | 从多次事件归纳，可由用户审阅 |
| 情感记忆 | 关系变化、承诺、创伤、偏好与未决心结 | 按显著性和衰减规则维护 |

检索不能只做“当前输入与旧文本的向量相似度”。候选应综合人物、地点、时间距离、关系、情绪显著性、未决线索和当前意图，再给出可解释的入选理由。VCPToolBox 的 TagMemo/RiverMemo 与 AIO Recall 提供了语义拓扑和产品化监控素材，SillyTavern Vector Storage 提供生成前历史重排范式；对应梳理见 [特色功能贡献统计](./AI客户端特色功能贡献统计.md#角色上下文与长期认知) 与 [SillyTavern 独特功能调查](./独特功能/SillyTavern-独特功能调查笔记.md)。

空闲整理采用“梦境”隐喻，但产物分级处理：补索引和生成摘要可以自动提交；合并、删除、改变稳定信念或关系等高影响操作进入审阅箱。这个闸门直接借鉴 VCPToolBox AgentDream 的待审批记忆操作，避免后台整理悄悄改写人格。

## 两套界面，同一事实源

### 沉浸模式

默认界面是一座简洁舞台：场景背景占据主视觉，在场角色立绘按空间关系排布，正文只显示叙事、台词和必要的剧情交互。模型思考、检索命中、工具载荷和审校意见全部留在幕后。

输入框提供四种明确语义：

- **说**：玩家角色公开说话；
- **做**：尝试动作，可触发规则裁决；
- **想**：只进入玩家角色内心，不被其他角色感知；
- **导演**：OOC 调整节奏、镜头、边界或期望，不写入世界事实。

消息仍是结构化 parts：正文、对白、动作、检定、媒体和可执行小部件分别建模。普通文本走稳定 AST；骰子、地图、证物板或小游戏进入隔离的可执行 part，不能访问宿主 DOM、密钥或完整文件系统。该组合直接承接 [消息渲染器横向对比的推荐架构](./消息渲染器/消息渲染器横向对比.md#推荐的组合架构)。

角色媒体采用 Risuai 的闭环思路：角色输出结构化 emotion/cue，优先直接匹配已有立绘，缺失时用轻量分类或嵌入回退。群像可按当前发言者和空间位置分屏。图片生成只在转场、服装永久变化或用户点播时异步发生，不阻塞每一条回复；情绪链依据见 [Risuai 独特功能调查](./独特功能/Risuai-独特功能调查笔记.md#能力一emotion-images情绪图主链确认静态证据)。

### 导演室

导演室用于创作和排错，提供：

- 当前世界状态、时间、地点和在场者；
- 每名角色“知道 / 相信 / 隐瞒 / 不知道”的差异视图；
- 当前时间线、候选回复、分支树和 canon 固定点；
- 本轮上下文的来源、Token 预算、世界书命中和记忆召回解释；
- 未决线索、关系网、剧情时钟与场景节拍；
- 后台记忆整理的接受、修改和拒绝入口；
- 单次重演时可锁定事实、锁定某角色表演或只重做媒体。

AIO Hub 的同源上下文预览、Token/宏/变量状态和请求检查器适合作为这个工作台的原型；SillyTavern 的 prompt 分项统计和 regex debugger 可补足内容作者视角。相关编译对象见 [AIO Hub 对话请求与上下文调查](./对话请求与上下文/AIO-Hub-对话请求与上下文调查笔记.md) 与 [SillyTavern 对话请求与上下文调查](./对话请求与上下文/SillyTavern-对话请求与上下文调查笔记.md)。

## 分支与重演语义

极致 RP 需要同时支持“挑一句更好的台词”和“走另一条人生”，二者不能混为同一种 regenerate：

| 操作 | 语义 | 后果 |
|---|---|---|
| 候选重演 | 当前回复尚未确认，重新表演同一已知状态 | 不创建新世界分支；保留候选 swipe |
| 局部重演 | 锁定事实，只改台词、文风、情绪或媒体 | 状态 delta 不变 |
| 事件撤回 | 删除刚提交且尚无后继依赖的事件 | 原子回滚状态和认知投影 |
| 时间线分叉 | 从已有历史点选择另一行动 | 新 timeline id，旧线完整保留 |
| Canon 合并 | 从另一分支摘取不冲突的设定或素材 | 通过冲突检查后生成显式合并事件 |

长会话压缩只生成新的阅读投影，不删除事件日志。摘要必须能追溯到源事件；角色回忆出错时，用户可以判断是角色记错、召回漏掉，还是摘要失真。

## 多人共演

多人模式以房主的世界事件日志为权威，每名玩家只接收其角色可感知的投影。公开台词、私聊、内心和 OOC 使用不同通道；断线重连按事件游标补齐。回退、切换 canon 和高影响世界编辑需要房主或预设表决规则确认。

Risuai 的 WebRTC 房间验证了轻量实时共享角色与聊天的入口，但其房间缺少持久化、撤销和完整权限层。本构想保留 P2P 作为局域或好友模式，同时增加主机权威、加密身份、事件游标和分支协商。现状依据见 [特色功能贡献统计](./AI客户端特色功能贡献统计.md#会话演化连续性与轨迹交付)。

## 内容与运行时边界

角色演员默认只获得剧情域工具：观察、检定、移动、交互、物品、关系和场景信号。文件系统、浏览器、桌面自动化和任意网络访问不应因角色卡导入自动开放。

内容扩展分三档：

| 档位 | 能力 | 适合对象 |
|---|---|---|
| 数据 | 角色卡、世界书、规则表、媒体与提示模板 | 默认可移植内容 |
| 沙箱脚本 | 受限计算、UI 小部件和结构化剧情工具 | 骰子、地图、谜题、战斗面板 |
| 本机能力 | 文件、网络、外部程序和设备 | 仅受信世界包，逐项授权 |

SillyTavern 允许内容资产定义工具，Risuai 提供 CSP iframe + postMessage RPC 的插件安全分层，消息渲染横向对比则给出了 prose/active/trusted 运行域。目标方案组合这三点，但任何导入资产都不能自动晋升为本机能力。相关能力分布见 [Agent 工具横向对比](./Agent工具/Agent工具横向对比.md) 与 [消息渲染器横向对比的极致自由型组合](./消息渲染器/消息渲染器横向对比.md#极致自由型组合)。

## 应主动舍弃的拼装方式

1. **不把全部机制塞回一条 Markdown 字符串。** 情绪、工具、检定、状态变化和可执行内容使用 typed parts 与结构化事件。
2. **不让所有角色共享完整历史。** 群聊的核心是认知隔离，独立人格提示词只是起点。
3. **不在每轮自动出图。** 高频媒体生成会抬高延迟并造成角色外观漂移；优先复用角色资产，转场时再生成。
4. **不把摘要当事实源。** 摘要是可再生投影，事件日志和显式状态才是权威。
5. **不把后台 Agent 讨论展示成剧情。** 导演、裁判和编辑的过程默认折叠，仅在导演室按需查看。
6. **不让角色卡隐式携带高权限。** 内容兼容、字段生效、脚本执行和本机授权是四个独立层级。
7. **不采用破坏性 regenerate。** 任何已经影响后续世界的版本都通过分支保留。

## 一段具体体验

玩家在宴会场景选择“想”，输入：“我认出了那枚戒指，但不能让伊芙知道。”

系统只把这条信息写入玩家角色的私有认知。导演发现当前冲突适合压低节奏，让伊芙继续交谈并安排远处守卫靠近；伊芙的演员看不到戒指秘密，只能依据玩家的停顿和此前关系推断不安。她的回复携带 `emotion=疑惑` 与“向前半步”的动作提议。规则层确认双方距离允许，连续性编辑确认她没有说出戒指来历，舞台切换疑惑立绘并压低背景音乐。

回合提交后，世界事实只记录玩家看见戒指、玩家停顿、伊芙靠近和守卫接近；“戒指属于失踪王储”仍是玩家的信念，不会被自动升级为世界真相。十章之后，伊芙可以回忆玩家当时的异常，却仍不知道秘密内容，直到另一条事件真正把它告诉她。

这类“信息没有泄漏，但关系留下了痕迹”的连续性，才是本构想最值得做的部分。

## 拼装来源与改造

| 已调查素材 | 直接借用 | 为极致 RP 做的改造 |
|---|---|---|
| SillyTavern | Character Card、World Info、swipe、提示词分区、内容脚本生态 | 增加角色认知隔离；把已产生后果的重生成升级为时间线分叉 |
| AIO Hub | 一体化 Agent 配方、上下文处理管道、Recall、资产协议、同源预览 | 从“给一个 Agent 编译上下文”扩成“按每个角色认知分别编译” |
| Risuai | 模型驱动情绪媒体、群聊立绘、多格式角色资产、实时共享入口 | 情绪改用结构化 cue；联机改为主机权威的事件同步 |
| VCPToolBox | 语义拓扑记忆、递归召回、AgentDream、跨 Agent 持久通信 | 记忆按事实/信念分层；后台合并和删除必须审批；多 Agent 隐入幕后 |
| VCPChat | 可执行消息、主动 Topic、桌面/声音运行时 | 剧情小部件进入隔离 part；主动性受导演节拍和场景规则约束 |
| Cherry Studio / AIO Hub | 持久消息树和活动分支 | 消息分支与世界时间线建立明确映射 |
| Hermes Agent / Pi | fork、lineage、压缩后可追溯 | 摘要只做投影，始终能寻址源事件 |
| 消息渲染横向方案 | typed parts、稳定流式 AST、隔离 Artifact、持久化分流 | 只向玩家投影舞台内容，后台过程进入导演室 |

这些机制的项目归属和证据强度可从 [AI 客户端特色功能贡献统计](./AI客户端特色功能贡献统计.md) 继续追到各单项目笔记。表格第三列均为本构想的新设计，不能反推为来源项目已经实现。

## 实现顺序

### 第一阶段：先闭合叙事事实

- Character Card + 世界书导入；
- 单角色与玩家档案；
- 事件日志、状态投影、候选 swipe、时间线 fork；
- 角色认知隔离与基础记忆召回；
- 上下文分析器；
- 结构化文本/动作/情绪消息和已有立绘切换。

这一阶段不做通用工具生态、实时联机和每轮生成媒体。验收重点是 200 回合后仍能复查“谁在何时知道什么”。

### 第二阶段：形成群像导演系统

- 多角色导演与按需演员调度；
- 地点、时钟、物品、关系和未决线索；
- 连续性审校与状态冲突处理；
- 场景结束整理、可审批的梦境记忆；
- 导演室、分支树和局部重演。

### 第三阶段：补齐舞台与生态

- 语音、音乐、背景和异步图片生成；
- 沙箱剧情部件、规则包和内容市场；
- 多人共演、秘密通道和断线重放；
- 世界包版本迁移、跨战役角色成长与可分享时间线。

## 验收指标

极致 RP 不宜用“功能数量”验收。至少建立以下回放测试：

| 指标 | 测试方法 |
|---|---|
| 角色一致性 | 在长会话不同位置重复施加相同价值冲突，比较选择与语言特征 |
| 知识泄漏率 | 为每个角色埋入互斥秘密，统计未传播前的越权引用 |
| 世界矛盾率 | 对事件前置条件、物品唯一性、位置和时间做确定性重放校验 |
| 记忆命中质量 | 对人工标注的关键旧事件测召回、误召回和来源可解释性 |
| 分支隔离 | 从同一节点建立多线，确认状态、记忆和媒体资产不会串线 |
| 叙事延迟 | 分别记录首个舞台反馈、首段正文、状态提交和媒体完成时间 |
| 重放一致性 | 从事件日志重建战役，比较状态哈希与用户可见消息版本 |

这套产品最终依靠的是**世界事实、角色认知、叙事表演和舞台呈现之间稳定且可解释的转换链**。角色卡和世界书提供生态入口，记忆与事件系统守住长期连续性，导演系统负责群像节奏，媒体层只做忠实投影。四层同时成立，RP 才能从“很会聊的角色”进化为“可以长期生活的世界”。
