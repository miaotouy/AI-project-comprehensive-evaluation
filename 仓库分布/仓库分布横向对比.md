# AI 项目仓库分布横向对比

> 对比对象：AIO Hub、AstrBot、Chatbox、Cherry Studio、DeepChat、DeepSeek Harness、Hermes Agent、Jan、LobeHub、Manifold Desktop、NextChat、Open WebUI、OpenCode、Pi、Risuai、SillyTavern、VCPChat、VCPToolBox
>
> 对比更新日期：2026-08-17
>
> 依据：本目录 18 份单项目仓库分布调查笔记及各项目笔记所列 HEAD 代码快照；`统计仓库.ps1` 在 UTF-8 控制台下对 18 个仓库重跑复核；并参考对话请求、消息渲染器、Agent 工具、生成式运行时和外部协作等类别的既有调查笔记；AIO Hub 的早期产品沿革由用户补充，未以公开提交历史或作者访谈材料独立复核
>
> 对比方法：只统计 Git 跟踪文件，按统一扩展名、目录与文件名规则计算源码、语言、文档和测试，再复核 workspace、构建配置与平台入口；推测部分以仓库结构和其他类别已确认主链互相校验，不反推作者真实动机
>
> 对比范围：仓库形态、模块量级、语言、文档、测试、跨平台组织，以及结构对后续功能生长的可能影响；不评价代码质量，不以行数推算工作量，未运行全量构建与测试
>
> 文档定位：实现学习与跨项目横向比较，不作为整改方案

## 结论摘要

这些项目的仓库形态可以分为四组：以 Electron/Tauri 为中心的多端客户端、前后端合仓的 Web 服务、按能力拆包的 Agent/CLI monorepo，以及插件/附属程序集合。规模最大的 LobeHub 和 Hermes Agent 都接近 200 万源码行。LobeHub 以统一的 TypeScript 应用与业务包为主，Hermes Agent 则混合了 Python Agent 核心、TypeScript 多种 UI、测试、技能和文档；两者的目录职责并不相同。Risuai 落在四组之间：单包 SPA 加多个薄平台壳（Tauri 桌面、Node 自托管、Capacitor 移动配置），同一份业务代码靠运行时探测区分平台，平台差异由探测逻辑而非构建分支承担。

文件数同样不能直接代表源码规模。Open WebUI 的 5,031 个跟踪文件中有 3,870 个位于 `static/`；VCPToolBox 跟踪了 AdminPanel 的 `vendor` 与 `dist`；VCPChat 最大源码区域是 `vendor`；OpenCode 的文档主要是多语言文档站；Hermes Agent 的大量文档来自 skills；Risuai 跟踪约 80 MB 的 tokenizer 模型数据，贡献的是字节量与存储而非源码行。横向理解仓库必须同时看源码行、目录用途和被跟踪的配套资产。

测试数量只表示仓内可见测试资产，不表示覆盖率。Hermes Agent、LobeHub、Cherry Studio、DeepChat、Pi 和 OpenCode 有大规模、按模块分布的测试；Open WebUI 虽声明 Pytest/Vitest 依赖，本快照只识别到 2 个跟踪测试文件；Manifold Desktop 未识别到显式测试树；Risuai 的 25 个测试文件全部为 Vitest 单元测试，无端到端。这些是仓库结构事实，不足以推导运行质量。

结合其他类别笔记，可以做出一个范围有限的推测：仓库结构通常与产品当前的主要增长方向互相适配。共享 Web 前端有利于把聊天和设置同步带到多端，服务端合仓有利于增加账号、协作和任务治理，能力拆包有利于增加客户端或协议入口，插件/附属进程集合则有利于接入异构工具。相应成本会落在平台适配、跨层协议、包间 API 或插件治理上。第 3 节逐项说明这些可能影响；除已单列来源的 AIO Hub 早期沿革外，本文不据现状推断作者的真实选型动机或未来路线图。

DeepSeek Harness 是本次对比中插件颗粒度最细的 TypeScript monorepo 插件集合（pnpm workspace + tsc -b 双编译面 + tsdown + vitest）：226 个 `@deepseek-ai/dsh-*` 包分属 46 组。7,412 个跟踪文件中源码 2,756 文件 / 590,509 行，TypeScript 占 95.5%，最大区域是 `packages/client` 组（1,140 文件 / 153,754 行）。框架运行时以 `vendor/` 9 包 6,566 行 vendored Cordis 完全持有，pi-ai（`@earendil-works/pi-ai@0.82.1`）则走 npm 依赖而非 vendored；文档 2,421 文件、测试 1,772 文件，四个运行时（Node、浏览器 React、Python SDK、Landlock C）各有独立对外边界。

## 1. 总体量级

| 项目 | 跟踪文件 | 源码文件 / 物理行 | 文档文件 / 物理行 | 测试文件 / 测试源码行 | 测试文件 / 源码文件 |
| --- | ---: | ---: | ---: | ---: | ---: |
| AIO Hub | 3,003 | 2,365 / 727,591 | 372 / 73,314 | 343 / 60,405 | 14.5% |
| AstrBot | 1,623 | 958 / 302,751 | 441 / 31,728 | 186 / 60,907 | 19.4% |
| Chatbox | 1,414 | 1,168 / 215,215 | 58 / 13,148 | 282 / 49,839 | 24.1% |
| Cherry Studio | 8,002 | 6,804 / 1,116,566 | 514 / 71,247 | 1,898 / 506,552 | 27.9% |
| DeepChat | 4,379 | 3,052 / 904,777 | 282 / 52,977 | 953 / 366,497 | 31.2% |
| DeepSeek Harness | 7,412 | 2,756 / 590,509 | 2,421 / 170,252 | 1,772 / 307,244 | 64.3% |
| Hermes Agent | 8,748 | 6,270 / 2,094,425 | 1,528 / 462,283 | 3,604 / 821,783 | 57.5% |
| Jan | 2,258 | 1,105 / 191,672 | 157 / 16,735 | 319 / 60,401 | 28.9% |
| LobeHub | 14,527 | 12,077 / 1,982,207 | 796 / 162,035 | 3,032 / 806,041 | 25.1% |
| Manifold Desktop | 127 | 98 / 35,496 | 5 / 553 | 0 / 0 | 0.0% |
| NextChat | 425 | 220 / 48,347 | 29 / 3,566 | 34 / 1,347 | 15.5% |
| Open WebUI | 5,031 | 1,011 / 346,049 | 11 / 6,211 | 2 / 43 | 0.2% |
| OpenCode | 6,510 | 3,580 / 721,582 | 820 / 230,083 | 955 / 188,690 | 26.7% |
| Pi | 1,366 | 1,176 / 268,499 | 97 / 32,624 | 500 / 114,906 | 42.5% |
| Risuai | 584 | 412 / 126,194 | 13 / 2,810 | 25 / 4,147 | 6.1% |
| SillyTavern | 988 | 527 / 223,385 | 19 / 1,009 | 23 / 9,996 | 4.4% |
| VCPChat | 894 | 598 / 334,822 | 48 / 9,919 | 24 / 6,543 | 4.0% |
| VCPToolBox | 3,010 | 1,283 / 459,871 | 692 / 161,572 | 32 / 7,056 | 2.5% |

注：aio-hub、VCPChat、VCPToolBox 三行按各单项目笔记口径。其中 VCPChat 的源码排除 vendor/测试/文档，文档为 `*.md` 且不含 vendor/测试内，测试含 `tests/` 与 `SovitsTest/`，其 vendor 文本（22 文件 / 42,870 行）单列。口径差异与统一脚本对照值见第 8 节。

“测试文件/源码文件”可帮助观察测试资产是否为仓库的主要组成，但分母与分子并不互斥：共置的 `.test.ts` 既是源码文件也是测试文件，夹具和快照可能只进入测试文件数。该比例不能当作覆盖率。

## 2. 仓库形态与主要模块

| 项目 | 仓库形态 | 最大的三个源码区域（物理行） |
| --- | --- | --- |
| AIO Hub | Bun workspace；Vue/Tauri 桌面 + 独立 Tauri 移动端 | `src/tools` 448,538；`mobile/src` 57,597；`src-tauri/src` 53,904 |
| AstrBot | Python 服务 + 合仓 Vue Dashboard | `astrbot/core` 107,147；`dashboard/src` 94,974；`tests` 区合计约 59,836 |
| Chatbox | Electron 单应用；Web/Capacitor 共用 renderer/shared | `src/renderer` 150,097；`src/shared` 34,046；`src/main` 22,533 |
| Cherry Studio | Electron 主应用 + pnpm 内部包 | `src/renderer` 495,622；`src/main` 432,627；`packages/ui` 80,009 |
| DeepChat | Electron/Vue 应用 + 插件运行时 + 独立测试树 | `src/main` 282,560；`test/main` 265,279；`src/renderer` 136,575 |
| DeepSeek Harness | TypeScript monorepo（插件集合）；pnpm workspace + tsc -b 双编译面 + tsdown + vitest | `packages/client` 组 153,754；`core` 40,745；`subagent` 24,486 |
| Hermes Agent | Python/npm 混合 monorepo；CLI/gateway/tools + Desktop/Web/TUI | `apps/desktop` 324,484；`hermes_cli` 196,956；`tests/gateway` 151,809 |
| Jan | Yarn workspace；Web 前端 + Tauri + 扩展包 | `web-app/src` 119,779；`src-tauri/src` 21,539；`src-tauri/plugins` 13,858 |
| LobeHub | pnpm monorepo；Web、server、desktop、CLI 与业务包 | `apps/server` 388,241；`src/features` 353,088；`packages/database` 172,169 |
| Manifold Desktop | Visual C++ 单应用 + WebView2 前端 | `Manifold.Core` 28,939（根层头文件约 26,533）；`frontend` 4,837；`server/src` 259 |
| NextChat | Next.js 单应用 + Tauri 封装 | `app/components` 14,990；`app/locales` 13,184；`app/client` 4,566 |
| Open WebUI | Python 后端 + Svelte 前端 + 大型静态资源树 | `backend/open_webui` 196,710；`src/lib` 141,095；`src/routes` 5,876 |
| OpenCode | Bun/TypeScript packages monorepo；CLI/TUI/Desktop/Web/Server/SDK | `packages/opencode` 176,788；`packages/app` 172,930；`packages/core` 67,546 |
| Pi | npm packages monorepo；Agent/LLM/TUI/CLI/协议库 | `packages/coding-agent` 128,619；`packages/ai` 60,607；`packages/tui` 33,873 |
| Risuai | 单包前端 + Tauri/Node/Capacitor 合仓 | `src/ts` 71,247；`src/lib` 36,646；`src/lang` 10,685 |
| SillyTavern | Node 服务 + 传统浏览器前端 | `public/scripts` 141,344；`public` 根源码 27,688；`src/endpoints` 22,786 |
| VCPChat | Electron 应用 + 功能目录 + Rust/附属进程；无统一 workspace | `ScriptoriumModules` 约 24,900 行（另含 65,411 行 JSON 诊断数据）；`rust_audio_engine`/`rust_chat_data_service` 合计 23,409 行；`VCPDistributedServer/Plugin` 147 个跟踪文件 |
| VCPToolBox | Node 服务 + Vue Admin + 插件/知识/原生工具集合 | `AdminPanel-Vue/src` 98,552；`AdminPanel-Vue/vendor` 67,369；`Plugin/SkillBridge` 37,409 |

Risuai 是本次对比中"单包前端 + 多壳合仓"的代表：根 package.json 为 private 单包，无 workspace 编排，src 目录占源码行 94.7%，TypeScript 65.5% + Svelte 28.8% 构成绝大部分语言占比；同一份 Svelte 5 SPA 靠运行时探测覆盖 Web、Tauri 桌面、Capacitor 移动与 Node 自托管，平台差异收敛在 platform.ts 判定与 Rust 命令层，不经过条件编译。唯一嵌套独立包是开发中的 server/hono（自带 package.json 与独立依赖锁），文档声明它将替换 server/node 的 Express 实现。测试 25 文件全部为 Vitest 单元测试，无端到端；另跟踪约 80 MB 的 tokenizer 模型数据，行数统计不受其影响。

### 结构模式

- **共享前端、多壳复用**：Chatbox、AIO Hub、Jan 和 NextChat 让 Web 技术前端被桌面或移动壳复用，但复用深度不同。Chatbox 把桌面/Web/Capacitor 差异收敛进 `src` 的平台层；AIO Hub 明确保留单独 `mobile/src`；Jan 共享 `web-app`，平台能力在 Tauri 插件与 feature 中分化；NextChat 的 Tauri 层最薄。Risuai 同一份 SPA 靠运行时探测覆盖 Web、Tauri 桌面与自托管，复用边界由探测逻辑承担，未设平台构建分支。
- **累积式工具箱**：AIO Hub 最初以工具为产品单位，聊天会话是在工具箱形成后加入的能力，当前 `src/tools` 占约 62% 源码，与这一沿革一致。其原名、正则应用器起源和 SillyTavern 背景等沿革细节见 3.2 节；代码快照只能确认当前结构和能力，无法独立复原早期历史。
- **主应用与内部包并重**：Cherry Studio、LobeHub、OpenCode、Pi。Cherry 的主进程/renderer 仍占绝对多数；LobeHub 与 OpenCode 已把数据库、模型运行时、工具和平台入口拆成大量包；Pi 的每个顶层包本身就是可说明、可测试的发布能力。
- **前后端同一发行单元**：AstrBot 与 Open WebUI 都把 Python 服务和 Web 前端合仓。AstrBot 打包时显式把 Dashboard 产物嵌入 Python 包；Open WebUI 的 Python wheel 强制包含前端 build。
- **扩展集合式仓库**：VCPToolBox、VCPChat、Hermes Agent 都含大量插件/skills/附属进程。Hermes 已有 Python/npm workspace 与明确应用边界；VCP 两仓更多依靠目录和进程协议组织，量级统计容易被 vendor、dist、知识与技能资料影响。

### 高占比目录的内部构成

下面把表中的大目录再向下拆一层。行数仍是当前快照的物理行近似值，用于解释“这块代码大概在做什么”，不用于重新定义模块边界；其中测试、翻译、生成文件和第三方资产会单独标明。

| 项目 | 高占比目录 | 目录内大致内容 |
| --- | --- | --- |
| AIO Hub | `src/tools`（448,538 行，约占全仓源码 62%） | 这里包含四十多个可独立进入的工具域。最大的是 `llm-chat`（约 100,386 行，聊天、上下文、工具调用和会话相关 UI/逻辑），其次是 `rich-text-renderer`（约 32,446 行）、`agent-manager`（约 31,276 行）、`recall`（约 24,517 行）；还包括媒体生成、OCR、知识库、正则、画布、VCP 连接等。每个工具目录通常把页面、状态、服务调用、类型和 Markdown 说明放在一起，所以实现与文档在此重叠。 |
| AstrBot | `astrbot/core`（107,130 行）与 `dashboard/src`（94,974 行） | `core` 是 Python 服务主体，`platform` 负责 IM/平台适配，`provider` 接模型和其他提供方，`agent`、`pipeline`、`message`、`tools`、`skills`、`knowledge_base` 分别承载 Agent、事件流水线、消息、工具、技能和知识库，`db/config/cron` 处理持久化、配置和定时任务。Dashboard 则按 `components`（43,210 行）和 `views`（28,791 行）组织管理界面，另有 API、stores、router、i18n，承担服务的控制和观测。 |
| Chatbox | `src/renderer`（150,097 行） | `components`（43,531 行）是页面组件，`packages`（30,064 行）是会话、模型调用、MCP、工具、技能和 token 估算等共享业务包，`routes` 与 `stores`（各约 20,600 行）负责页面路由和应用状态。`platform` 放 Web/Electron/Capacitor 差异；`dev/uiInventory.generated.ts` 单文件约 1.2 万行，是生成的 UI 清单，不应按业务模块理解。 |
| Cherry Studio | `src/main`（432,613 行）与 `src/renderer`（495,622 行） | 主进程中 `ai`（143,466 行）负责模型、Agent、MCP、消息和流式运行链，`data`（124,779 行）负责数据库、迁移、缓存和偏好，`services`（73,262 行）承载文件、窗口、更新等桌面服务，`features` 是跨服务功能编排。Renderer 主要由 `components`（225,142 行）和 `pages`（172,641 行）构成，页面覆盖聊天、Agent、知识库、文件、笔记、绘画和设置；两大目录分别对应桌面运行时与用户界面。 |
| DeepChat | `src/main`（282,560 行）与 `test/main`（265,279 行） | 主进程按能力拆成 `agent`、`provider`、`memory`、`remote`、`mcp`、`session`、`skill`、`tape` 等，覆盖 Agent 执行、模型适配、记忆/知识、远程控制、MCP、会话和可回放执行记录；`cli`、`approval`、`logging` 是本快照新增的本地控制面、审批和结构化日志。`test/main` 基本按同名子目录镜像这些能力，`agent`（56,906 行）、`memory`（34,273 行）等大块均为测试代码。 |
| DeepSeek Harness | `packages/client` 组（153,754 行，约占全仓源码 26%） | 组内是 48 个小包：39 个 `ui-*` 界面插件（`ui-primitives` 140 文件、`ui-conversation` 125 文件最大）加 runtime、connection、web-react 等支撑与壳层包，界面主体在组内，apps/web 只是 vite 壳。框架层的第三方影响有两个对照样本：`vendor/` 的 vendored Cordis（9 包 6,566 行）与以 npm 依赖消费的 pi-ai（`@earendil-works/pi-ai@0.82.1`）。 |
| Hermes Agent | `apps/desktop`（324,484 行）、`hermes_cli`（产品区域约 196,956 行）、`tests/gateway`（151,809 行） | Desktop 的 `src/app`（126,651 行）是多页面控制台，含 agents、chat、gateway、session、skills、settings 等界面；`electron`（42,946 行）负责启动 Python 后端、连接状态、平台探测和修复流程。`hermes_cli` 根下大量 Python 文件是 Agent/工具/会话/网关核心，子目录主要是子命令、代理路由、鉴权和观测；测试与 CLI 部分共置，另有独立 CLI 测试树。`tests/gateway` 的大头是按问题和场景编号的网关回归测试，不能与产品源码直接相加解释功能量。 |
| Jan | `web-app/src`（119,779 行） | `containers`（28,097 行）和 `hooks`（21,501 行）组成页面容器与交互状态，`lib`（20,917 行）放核心服务、请求和工具函数，`routes`、`components` 负责页面和通用组件，`services` 连接 Tauri/core 能力。`locales` 是 17 种语言资源，测试目录与组件共置；因此这是共享 Web 前端，不包含本地推理引擎本体，后者在扩展包/原生层。 |
| LobeHub | `apps/server`（388,241 行）、`src/features`（353,088 行）、`packages/database`（166,424 行） | Server 内部以 `services`（265,940 行）为主，集中账号、会话、模型、Agent、文件、知识、Connector 和任务等服务；`routers`（76,454 行）是 HTTP/API 边界。`src/features` 是 Web 端按用户功能拆分的页面域，最大的 `Conversation`（70,937 行）和 `ChatInput`（33,163 行）覆盖会话展示与输入，其后是 Settings、AgentTasks、ResourceManager 等。Database 的主体在 `models`（127,284 行），另有 repositories、schemas 和迁移/测试配套，说明数据模型是共享基础设施而非单个页面的附属物。 |
| Manifold Desktop | `Manifold.Core`（28,939 行） | 目录中约 26,533 行来自根层 C/C++ 头文件，最大单文件是第三方/通用 `json.hpp`（约 24,765 行）；自有实现主要是 `ProcessManager`、`SessionManager`、`SettingsManager`、`PromptManager`、凭据管理，以及 `Providers`、`MCP`、`Plugins` 适配目录。这部分行数主要反映头文件和原生平台核心，自有业务实现规模要小得多。 |
| NextChat | `app/components`（14,990 行）与 `app/locales`（13,184 行） | Components 根文件是聊天页、设置、导入导出、认证、绘图/语音等界面；`realtime-chat`、`sd`、`voice-print` 是较小的实时聊天、Stable Diffusion 和声纹功能分区。`locales` 基本是 21 个语言文件，行数主要来自翻译字典；Provider、会话和客户端状态等核心逻辑位于 `app/client`。 |
| Open WebUI | `backend/open_webui`（196,710 行）与 `src/lib`（141,095 行） | 后端的 `routers`（31,629 行）是 API，`utils`、`models`、`retrieval` 分别支撑通用服务、数据模型和知识检索；`migrations` 是数据库演进。其 `static/swagger-ui` 约 86,712 行，几乎全是 Swagger UI 的跟踪静态文件，属于发行资产。前端 `src/lib/components`（118,327 行）覆盖聊天、管理、工作区、自动化、文件和连接器等 Svelte 组件，`apis` 负责前端 API 封装。 |
| OpenCode | `packages/opencode`（176,787 行）、`packages/app`（172,930 行）、`packages/core`（67,546 行） | `opencode/src` 是 Agent/CLI/server 运行时，细分 session、provider、tool、permission、MCP、LSP、project、worktree 等；该包还有 `test`（95,134 行），测试已接近实现量。`app/src` 是跨桌面/Web 的 UI，`i18n`（73,924 行）占其源码大头，主要是多语言字典，另有 components/pages/context；`core/src` 提供数据库、文件系统、事件、凭据、权限和插件等共享底层，测试约 34,396 行。 |
| Pi | `packages/coding-agent`（128,619 行） | `src/core`（32,944 行）是 Agent session、模型配置、执行、消息、压缩和工具循环，`src/modes`（19,391 行）实现 interactive、RPC、print 等运行模式；`test`（49,416 行）和 `examples`（16,052 行）分别是回归测试与可运行示例。该包更像可嵌入的 coding-agent 产品包，测试/示例占比高于一般 GUI 应用。 |
| Risuai | `src`（119,540 行，约占全仓源码 94.7%） | `src/ts` 是业务层（71,247 行），`process` 聊天管线约 3 万行（request、mcp、memory 居前），`plugins`、`storage`、`parser`、`model`、`setting` 为子模块；`src/lib` 是 Svelte UI 组件（36,646 行），按 SideBars、Setting、ChatScreens、UI 等界面区域分目录；`src/lang` 为 7 个语言包（10,685 行）。约 80 MB 的 tokenizer 模型数据位于 `public/token`，属于被跟踪资源而非业务模块。 |
| SillyTavern | `public/scripts`（141,344 行） | 根层脚本（79,174 行）实现聊天、角色、世界书、设置、导入导出等长期积累的浏览器功能；`extensions`（44,943 行）按 memory、TTS、翻译、正则、向量、快速回复、Stable Diffusion 等社区/可选能力拆分；另有 macros、slash-commands、autocomplete。整体采用脚本与扩展目录组织，尚未按包或领域服务进一步拆分。 |
| VCPChat | `ScriptoriumModules`（约 24,913 行自有 JS/CSS/HTML）与 `modules`（约 68,198 行） | Scriptorium 是共笔文坊/文档工作台，包含可编排内容、分页、渲染、导出和 Agent 控制；同目录另有约 65,411 行字体诊断 JSON 数据。`modules` 则是 Electron 主应用的聊天、IPC、renderer、Loom WebCore 和服务目录，分别承载界面桥接、消息/文件管理及 Agent Web 控制。统计时要把数据文件、vendor 和附属插件与自有实现分开。 |
| VCPToolBox | `AdminPanel-Vue/src`（98,552 行）、`Plugin/SkillBridge`（37,409 行） | 管理台中 `views`（49,114 行）是配置、监控和资源页面，`components`、`features`、`api` 提供通用界面、功能域和服务调用。SkillBridge 的绝大部分源码行来自 `SKILL/` 下 11 个随仓技能包（约 37,185 行），内容混合技能说明、文档/PDF/表格/PPT 处理脚本、示例、模板、字体和部分构建产物；这些文件构成可分发能力资产，桥接器本身只有少量脚本和 manifest。 |

阅读这些目录时，应先区分三类信息：`services`、`components`、`features` 等通用命名在不同项目中承担的职责并不相同；测试树、语言包、技能文档、Swagger UI、生成清单和第三方头文件会显著放大目录行数；产品主链仍需结合入口和依赖方向判断，例如 LobeHub 的 server/services、Cherry 的 main/ai 与 renderer/pages、OpenCode 的 opencode/src。单看文件最多的子目录，容易把配套资产误当成业务实现。

## 3. 可能的选型动因与功能生长方向（推测）

本节主要依据仓库现状推测，不是提交历史或作者访谈结论。“可能的选型动因”表示现有产品能力与仓库结构之间的适配关系；“功能生长方向”用于判断新增能力在当前边界下更可能落到哪里、需要同时修改哪些层，不等同于路线图预测。AIO Hub 的早期沿革由用户补充，以下将已知背景与影响推测分开。代码侧旁证主要来自[对话请求与上下文横向对比](../对话请求与上下文/对话请求与上下文横向对比.md)、[消息渲染器横向对比](../消息渲染器/消息渲染器横向对比.md)、[Agent 工具横向对比](../Agent工具/Agent工具横向对比.md)、[生成式输出与运行时横向对比](../生成式输出与运行时/生成式输出与运行时横向对比.md)和[外部执行体与应用协作横向对比](../外部执行体与应用协作/外部执行体与应用协作横向对比.md)。

### 3.1 结构模式带来的路径依赖

| 结构选择 | 与当前产品形态的可能适配关系 | 对后续功能生长的可能影响 |
| --- | --- | --- |
| Web 前端复用到 Electron/Tauri/Capacitor | 适合先建立一致的聊天、设置和数据模型，再用壳层取得本地文件、窗口、更新与移动发布能力 | Provider、会话和大部分 UI 功能可以一次实现、多端复用；功能越依赖系统 API、后台常驻或移动生命周期，平台 adapter、条件分支和端间能力差异越会成为主要成本 |
| Electron 主进程与 renderer 合仓 | 适合桌面端同时快速扩展 UI、本地服务、文件系统、Agent 执行和多窗口能力 | 新功能容易沿 IPC 两侧纵向生长，桌面能力上线较直接；主进程、renderer、preload、持久化与测试常需同步修改，跨层契约会逐渐成为稳定性边界 |
| 前后端合仓并作为一个发行单元 | 适合自托管、多用户、机器人或管理后台产品，前端与服务端版本可以一起发布 | 鉴权、协作、任务、Connector 和管理能力更容易落在服务端统一治理；前后端状态协议、数据库迁移和部署兼容面会随功能数一起扩大 |
| 按能力拆包的 monorepo | 适合核心运行时、协议、SDK、UI 和多入口需要复用同一套类型与测试的产品 | 新客户端、新协议或新业务域可以形成独立 package/app，复用范围清楚；包间 API、依赖方向、版本协同和全仓构建成为长期维护对象 |
| 插件、skills 与附属进程集合 | 适合能力来源异构、需要低门槛接入脚本、原生工具、知识资产或外部执行体的系统 | 功能可通过新增插件或进程较快扩张，也允许不同语言各取所长；发现、配置、权限、日志、取消、升级和跨平台打包若没有统一契约，会随插件数量增加，不同插件组合的验证范围也会扩大 |
| 传统单应用与功能脚本目录 | 适合保持旧格式、社区扩展和直接修改页面行为的兼容性 | 增加局部功能的入口短，旧扩展也较少受包边界限制；共享状态、隐式调用顺序和字符串协议会积累为兼容面，使深层重构及一致测试更困难 |
| 平台专用原生核心 | 适合产品明确依赖某一操作系统、原生控件或系统集成，当前仓库也以该平台为主要目标 | 平台内能力可以直接利用原生 API；后期若加入跨平台、Web 复用和社区插件，通常需要新建抽象层或并行实现，单纯增加构建目标难以覆盖这些差异 |

框架名单本身不足以决定这些影响。仓库中是否已有稳定的消息/事件协议、插件契约、服务端事实源和平台 adapter，往往比“用了 Electron、Tauri 还是 Next.js”更能决定下一项功能要改多少层。

### 3.2 AIO Hub：已知沿革与结构影响

用户补充的早期沿革如下：AIO Hub 原名 **All in One Tools**，起点是作者用于安放个人想法的工具箱。具体做法通常是从已有在线服务或应用程序中抽取一项基础能力，再加入作者认为必要的交互或流程优化。最初的工具是正则应用器，它延续自 AIO 之前的一个 Python 小工具；正则预设编排在那个阶段已经存在。聊天模块是在工具箱之后加入的，直接原因同样是现有产品没有充分覆盖作者想要的用法。作者曾向 Cherry Studio 提交功能 PR，但其认为审批和质疑过程成本过高，后来选择在自己的产品中继续实现；作者同时有 SillyTavern 使用背景。

这段沿革可以解释当前仓库中几个仅靠框架选型不容易说明的结构信号：

- **`src/tools` 承担产品主干职责。** 它占约 44.9 万行、约为全仓可识别源码的 62%。聊天、正则、Recall、Canvas 和创作工作台继续以工具域进入同一仓库，符合“先形成独立能力，再逐步组合”的生长方式。
- **正则编排早于聊天。** 当前聊天主链中的请求正则、显示正则、预设消息、快捷动作和可调整上下文管道，可以理解为早期“规则可配置、步骤可编排”思路向聊天域的延伸。这里能由代码确认当前机制，但“由早期正则工具延伸而来”依据的是上述沿革信息。
- **聊天更像后来形成的能力汇合点。** 其他调查笔记已确认聊天会整合 Agent 配置、工具、世界书、Recall、正则、VCP 协议与可运行内容。仓库因而仍保留工具箱外形，但聊天逐渐承担跨工具编排与统一呈现职责。
- **SillyTavern 经验形成了明确的兼容方向。** 当前代码支持导入 SillyTavern 角色卡、Prompt Preset、正则脚本、Quick Reply 和世界书；这与作者的酒馆使用背景相吻合，也说明部分选型目标是保留这类资产的表达力，再重新组织编辑和运行体验。
- **自主实现的阈值较低。** Cherry Studio PR 经历只能说明作者在该次协作中认为沟通和审批成本高，不能据此评价 Cherry 的一般贡献流程。对 AIO 的影响推测是：当现有产品或上游协作无法容纳某项想法时，新增能力更可能直接进入 AIO，减少等待外部项目接纳的时间。这有利于快速试验，也会使 `src/tools`、共享协议和配置系统持续扩大。

据此，AIO Hub 后续功能的默认生长路径可能仍是“新增或吸收一个工具能力，再把其中可复用的配置、数据和运行链接入聊天/Agent”。AIO 的聊天能力是在工具箱基础上形成的，如今承担着多个工具共享模型、上下文、Agent 和渲染能力的集成职责。长期成本更可能集中在工具间契约、配置迁移、桌面/移动差异和跨工具一致性，而非单个聊天模块自身的复杂度。

### 3.3 其他项目的可能解释

| 项目 | 可能的选型原因（仅按现状推测） | 对后续功能生长的可能影响 |
| --- | --- | --- |
| AstrBot | Python 核心、事件流水线、平台适配器与嵌入式 Vue Dashboard，符合“机器人服务是主体，Web 是配置和观测面”的产品边界 | 新 IM 平台、事件阶段、插件和后台任务较容易沿 Python 服务扩展，Dashboard 随配置面增长；若转向重交互终端用户客户端，现有仓库仍缺少独立客户端状态与平台壳边界 |
| Chatbox | TypeScript 统一 Electron、Web 和 Capacitor，与消费级聊天功能需要快速覆盖多端相匹配；桌面能力集中在 main/preload，产品会话模型仍由应用拥有 | Provider、会话、分支和普通 UI 功能可以跨端同步生长；更深的文件工作区、后台 Agent 或系统自动化会把压力集中到平台层和桥接契约，端间等价性不再自然成立 |
| Cherry Studio | 大型 Electron main/renderer 加少量内部包，适合桌面优先地同时推进聊天、知识、文件、Agent Session 和本地集成，而不必先把每个业务域发布成独立包 | 新能力可以继续沿 main 服务、renderer feature 和结构化 message parts 纵向扩展；普通聊天与 Agent 已形成不同运行链，跨模式搜索、恢复、复制和展示会持续要求明确的转换边界 |
| DeepChat | Electron 主体、独立插件、平行测试树和多 renderer 入口，与其接入 ACP、IM/CLI 控制面、浏览器浮层和原生插件的方向相适配 | 外部 runtime、新控制表面和平台能力更可能分别进入 plugin、main service 或 renderer entry；扩展性较强，但 IPC、插件协议、资源数据库和多平台构建矩阵会成为共同改动面 |
| Hermes Agent | Python Agent/CLI/gateway 作为核心，TypeScript Desktop/Web/TUI 作为多种客户端，符合“一个 Agent runtime 被多个表面控制”的形态 | 工具、skills、会话与网关能力可以在 Python 侧统一增长，各 UI 独立演进；事件协议、会话身份和终态语义必须长期稳定，否则多个客户端会分别承担兼容成本。UI 不共享代码也意味着同一交互能力可能需要重复实现 |
| Jan | Tauri 原生层、共享 Web 前端、`core` 契约和按引擎/能力拆分的 extensions，适合本地推理引擎、RAG、下载与硬件能力持续替换或增加 | 新模型引擎和本地能力较自然地新增为扩展或 Tauri 插件；`@janhq/core` service hub 会承受越来越多跨扩展契约，移动端能否跟进取决于原生插件和二进制在各平台的可用性 |
| LobeHub | 全栈 TypeScript monorepo 将 Web、server、desktop、CLI、数据库、模型运行时和业务包放在同一依赖图中，与其多用户服务、Agent、Connector 和多客户端并行发展相适配 | 新业务域可以拆成 package、server service 或 app，类型与测试可全仓复用；相应地，数据库迁移、缓存、权限、包依赖和多部署形态会形成跨模块协调成本，局部功能更容易触及共享基础设施 |
| Manifold Desktop | Windows x64 原生 C++/WinUI 加轻量 WebView2，可能优先追求平台集成、较短调用链和较小发行边界；生态扩展与跨端复用并非当前仓库的主要结构信号 | Windows 原生能力可直接加入核心；若增加跨平台客户端、复杂消息对象或插件生态，当前薄前端和平台专用核心都需要引入新的状态/协议抽象，增长成本会比继续增加 Windows 单机功能高 |
| NextChat | Next.js/PWA 为主体、Tauri 只作薄壳，符合先覆盖 Web 部署和轻量桌面分发的取向 | Provider、会话、Mask 和 UI 功能可继续在单一前端状态机内快速增加；当功能需要本地工作区、长驻 Agent、多进程任务或可靠跨设备协同时，薄壳和客户端 store 会面临拆出服务层的压力 |
| Open WebUI | Python 后端与 Svelte 前端合仓、前端产物进入服务发行包，符合自托管、多用户和服务端工具编排的定位 | 账号、权限、模型、知识、任务和 Connector 会继续向后端集中，Web 端消费统一事件；服务器状态、消息表/history 双重表示和 Socket.IO 协议的组合面会随协作功能扩大 |
| OpenCode | 以 server/runtime 为中心拆出 core、app、desktop、web、SDK 和 plugin，符合“同一编码 Agent 会话由 CLI/TUI/Web/Desktop 多端消费”的设计 | 新客户端和协议入口可以复用 SQLite 会话与 SSE 事件，核心功能不必复制；事件版本、owner/replay、包 API 和本地 server 生命周期会成为所有表面的共同约束 |
| Pi | coding-agent、模型 API、TUI、agent runtime 和协议按可发布包拆分，仓库形态更接近可嵌入 Agent 工具箱；当前没有一体化 GUI 主包 | 新模型、工具循环、终端组件或宿主适配可在包级生长，也便于外部项目选用局部能力；若出现完整 GUI，它更可能成为新的消费端，公共包 API 的兼容性会比页面内部重构更重要 |
| Risuai | 单包 SPA 复用同一份业务代码，Web、Tauri 桌面、Node 自托管与 Capacitor 移动共用一条运行时探测链，符合低平台维护成本与多入口并存的取向 | 新能力多在业务层统一增长，平台差异继续收敛在探测层与壳层命令；移动端与自托管目前停留在配置/骨架层，转向重原生集成时需要补齐平台边界或独立工程 |
| SillyTavern | Node 服务与传统浏览器脚本合仓、功能按页面脚本和扩展约定组织，与长期兼容角色卡、World Info、正则和社区扩展的需求一致 | 新社区玩法可以通过脚本、字段、命令和扩展迅速接入；这些入口也会继续扩大隐式协议与全局状态面，使 typed message、统一 Provider 行为或大规模模块化迁移需要兼容旧资产 |
| VCPChat | Electron、功能目录、Rust 本地服务和附属工具并存而无统一 workspace；当前结构允许功能按独立目录或外部进程加入，原生或性能敏感部分也可单独下沉 | 文档工作台、WebCore、音频、分布式服务等能力仍可通过新目录/进程生长；目录、IPC、文本协议、资源生命周期和打包规则会继续叠加，跨平台一致性与端到端测试成本可能高于单项实现成本 |
| VCPToolBox | Node 编排服务、Vue 管理台、插件/skills/knowledge 与 Rust/Python 辅助程序同仓，符合“能力供给层”需要接受多语言工具和内容资产的特点 | 新能力最容易以插件、外部执行 job、知识包或原生辅助程序加入；插件身份、权限、取消、产物回流、可观测性和二进制分发若继续分散实现，会成为规模扩大后的主要治理问题 |

### 3.4 DeepSeek Harness

DeepSeek Harness 是 DeepSeek AI 官方的 agent harness（产品命令 `dsh`），以“一切皆插件”为架构主张：框架运行时是 vendored 并 rescoped 的 Cordis（`vendor/` 9 包、6,566 行、18 项本地修改登记），产品逻辑拆成 46 组、226 个可独立发布的 `@deepseek-ai/dsh-*` npm 包。仓库是单一 TypeScript monorepo（pnpm workspace + tsc -b 双编译面 + tsdown + vitest），7,412 个跟踪文件中源码 2,756 文件 / 590,509 行，TypeScript 占 95.5%；文档 2,421 份、测试 1,772 个文件，226 个包全部自带 tests/ 区。四个运行面（CLI、Web GUI、Python SDK、Linux Landlock C）各有独立边界，平台差异按同缝隙多 provider 包组织而非条件编译。pi-ai（0.82.1）以 npm 依赖消费，与 Cordis 的 vendoring 形成对照。新能力预计以新包沿三件套进入，成本集中在包间 API 与 vendored 上游同步。

这些推测说明，仓库结构会影响功能的默认落点，但产品方向仍取决于后续能力和已有契约。实际形成约束的是已经被数据、插件或多个客户端依赖的接口，例如消息 parts、SSE/IPC 事件、扩展 service hub、VCP 文本标记、平台 adapter 和数据库 schema。新功能沿这些契约扩展时可以获得复用；一旦越过原有事实源、权限或运行时边界，结构调整成本就会明显增加。

## 4. 语言分布

| 项目 | 前三种语言（占可识别源码物理行） | 主要分工 |
| --- | --- | --- |
| AIO Hub | TypeScript 46.5%；Vue 43.9%；Rust 8.5% | 共享逻辑/业务逻辑；组件模板；Tauri 原生层 |
| AstrBot | Python 66.9%；Vue 23.6%；TypeScript 5.8% | 服务核心；Dashboard UI；前端逻辑 |
| Chatbox | TypeScript 96.9%；JavaScript 2.4%；CSS 0.4% | Electron/Web/移动共享业务高度统一 |
| Cherry Studio | TypeScript 98.5%；JavaScript 0.6%；CSS 0.4% | 主进程、renderer 与内部包统一 |
| DeepChat | TypeScript 79.9%；Vue 11.0%；JavaScript 3.7% | 主进程；UI；脚本/插件；另有 Swift 2.8% |
| DeepSeek Harness | TypeScript 95.5%；CSS 2.8%；JavaScript 0.8% | 产品包/scripts/测试统一 TS；UI 样式独立 CSS；Python 与 C/C++ 为独立对外边界 |
| Hermes Agent | Python 74.9%；TypeScript 22.0%；JavaScript 1.0% | Agent/CLI/tools；Desktop/Web/TUI |
| Jan | TypeScript 76.8%；Rust 19.0%；Swift 1.2% | Web/扩展；Tauri/原生插件；Apple 辅助层 |
| LobeHub | TypeScript 98.8%；Shell 0.3%；JavaScript 0.3% | 全栈应用与包高度统一 |
| Manifold Desktop | C/C++ Header 73.9%；C++ 11.7%；JavaScript 11.2% | WinUI 核心；原生实现；WebView2 UI |
| NextChat | TypeScript 88.3%；SCSS 10.4%；JavaScript 0.7% | Next.js 应用与样式 |
| Open WebUI | Svelte 35.3%；Python 31.7%；JavaScript 22.5% | 前端组件；后端；前端脚本 |
| OpenCode | TypeScript 93.5%；CSS 5.9%；Astro 0.2% | 应用/包；共享 UI；文档站 |
| Pi | TypeScript 95.6%；JavaScript 3.5%；CSS 0.4% | 核心包统一，少量构建/示例脚本 |
| Risuai | TypeScript 65.5%；Svelte 28.8%；JavaScript 4.2% | 业务逻辑/文案；UI 组件；服务器与代理脚本 |
| SillyTavern | JavaScript 86.1%；CSS 7.0%；HTML 6.0% | 浏览器与 Node 共享 JS 技术栈 |
| VCPChat | JavaScript 49.6%；CSS 12.8%；Rust 7.2% | Electron/模块；多主题 UI；本地服务 |
| VCPToolBox | JavaScript 51.1%；Vue 14.9%；Rust 8.6% | 服务/插件；管理台；检索与辅助程序 |

注：VCPChat 语言占比按单项目笔记口径（自有源码，剔除 vendor/测试/文档；基数含 JSON 数据），见第 8 节。

语言占比由物理行计算，模板文件按主扩展名整体归类。C/C++ Header 的高占比、Vue/Svelte 单文件组件以及翻译数据进入 TypeScript 源文件等情况，都使跨语言“生产率”比较没有意义；本表只回答代码放在哪里、由什么运行时承载。

## 5. 文档分布

文档组织大致有六种：

| 类型 | 代表项目 | 可观察分布 |
| --- | --- | --- |
| 独立用户/开发文档树 | AIO Hub、AstrBot、LobeHub、Jan | `docs` 按 user guide、architecture、self-hosting 或语言拆分 |
| 文档站及翻译副本 | OpenCode、Hermes Agent | OpenCode 的 `packages/web` 有 616 份文档；Hermes 的 `website/docs` 与 `website/i18n` 共 709 份 |
| Skills/知识本身就是内容资产 | Hermes Agent、VCPToolBox | skills、optional-skills、SkillBridge、knowledge 贡献大量文档，其中一部分是可执行技能、示例和随包资料，不属于开发说明 |
| 文档与实现共置 | AIO Hub、Cherry Studio | 工具说明、主进程说明或 Agent 指令进入源码/隐藏目录 |
| 仓内只保留入口说明 | Open WebUI、SillyTavern、Manifold Desktop、Risuai | 当前仓的 Markdown/TXT 数量少，完整文档可能在外部站点或尚未展开；Risuai 的用户手册在 GitHub Wiki，插件开发指南与迁移说明留在仓内 |
| 规范文档树与笔记/翻译资产结合 | DeepSeek Harness | docs/ 以 md + zh.md + i18n.yaml 三件套双语配对并投影网站；.agents/notes 按 implemented/archived/proposed/rejected 状态归档 1,386 篇；226 个包各带 README 三件套 |

特别的统计偏差包括：LobeHub 的两个 changelog 文件合计约 4.8 万行；Cherry Studio 的 `v2-refactor-temp`（166 份）与 `.agents/skills`（87 份）合计 253 份；AstrBot 的 441 份文档中有 200 份 changelog；VCPToolBox 的 692 份文档中，SkillBridge 和 knowledge 占多数。Risuai 的 13 份文档几乎全部位于根目录（plugins.md 单文件约 1.6 千行），应用内帮助以非 Markdown 的 `.cbs` 格式存于 `src/etc/docs`，不计入文档统计。

## 6. 测试分布

| 分布形态 | 项目 | 观察 |
| --- | --- | --- |
| 大型独立测试树 | Hermes Agent、DeepChat | 测试按 gateway/tools/CLI/agent 或 main/renderer/E2E 分区，测试本身是主要模块 |
| 与实现共置 | Cherry Studio、Chatbox、Jan、LobeHub、Risuai | 测试大量位于 `src` 或具体 package 内，能直接看到主要模块对应关系；Risuai 规模最小（25 文件 / 4,147 行），全部为 Vitest 单元测试，无端到端 |
| 按发布包自包含 | Pi、OpenCode | coding-agent/AI/TUI、opencode/core/app/LLM 等包各自维护测试 |
| 中小型集中测试目录 | AstrBot、NextChat、SillyTavern | 有统一 `tests`/`test`，但与产品源码量级差异较大 |
| 本次扫描很少或未找到 | Open WebUI、VCPChat、VCPToolBox、Manifold Desktop | 分别识别到 2、24、32、0 个测试文件（VCPChat 按笔记口径含 `tests/` 与 `SovitsTest/`）；只描述当前跟踪快照 |
| 按发布包自包含，兼有仓库级自检 | DeepSeek Harness | 226 个包全部自带 tests/ 区（含 client 组全部 48 包）；另有 apps 组装级 e2e、examples 快照回放、scripts 仓库自检三类跨模块测试 |

本调查不读取覆盖率产物，因为覆盖率通常是未跟踪的运行结果，也不能在未执行同一测试矩阵时横向比较。测试行数较高还可能包括评估数据、夹具、mock 和快照。

## 7. 跨平台与发布组织

| 项目 | 本仓包含的平台/形态 | 组织方式 |
| --- | --- | --- |
| AIO Hub | Windows/macOS/Linux 桌面；Android/iOS | 桌面 `src`+`src-tauri`，移动 `mobile/src`+独立 Tauri 后端 |
| AstrBot | 跨系统服务、Web Dashboard、Docker | Python 服务与 Vue Dashboard 合并发行，无原生客户端 |
| Chatbox | Web；三桌面系统；iOS/Android | renderer/shared 复用，Electron main 与 Capacitor 平台层分开 |
| Cherry Studio | Windows/macOS/Linux 桌面 | 单 Electron 应用按 OS/架构打包 |
| DeepChat | Windows/macOS/Linux 桌面 | Electron 主体，插件和原生运行时也按平台打包；本地控制平面 CLI 随构建产出 |
| DeepSeek Harness | 跨系统 CLI + Web GUI（React + 本地 HTTP 服务端）；Python SDK、Linux Landlock C 原生入口 | 四运行时各有独立边界；平台差异按同缝隙多 provider 包组织（sandbox、shell、session 等），非条件编译；无移动端 |
| Hermes Agent | Linux/macOS/Windows/WSL/Termux；CLI/TUI/Web/Desktop | Python 核心 + 多个独立 UI/app workspace |
| Jan | Windows/macOS/Linux；iOS/Android 构建入口 | Web 前端共享，Tauri feature 与插件承载平台差异 |
| LobeHub | Web/自托管服务、Docker、Desktop、CLI | server/desktop/cli 独立 app，共享 packages 与主前端 |
| Manifold Desktop | Windows 10+ x64 | Visual C++/WinUI + WebView2，平台专用 |
| NextChat | Web/PWA；Windows/macOS/Linux 桌面 | Next.js 主应用 + 薄 Tauri 壳；iOS 源码不在本仓 |
| Open WebUI | Web/PWA、pip/Docker/Kubernetes 服务 | 响应式 Web；README 所列原生桌面端是外部仓库 |
| OpenCode | Windows/macOS/Linux CLI/TUI 与 Desktop；Web | 多个平台入口共享 core、app、UI 与协议包 |
| Pi | 跨系统 Node/Bun CLI/TUI/服务 | 包级抽象，无本仓原生 GUI；隔离方案按环境选择 |
| Risuai | Web/PWA；Windows/macOS/Linux 桌面（Tauri）；Node 自托管（Docker）；移动（Capacitor 配置） | 单一 SPA 运行时探测区分平台，Tauri 命令层补桌面能力，Cloudflare Functions 代理与 Node 服务器分属 Web 与自托管；移动仅静态配置，无原生工程 |
| SillyTavern | 跨系统 Node/Web、Docker、Electron 启动器 | 同一浏览器 UI，Electron 只包装服务入口 |
| VCPChat | Electron 配置覆盖 macOS/Windows/Linux | 主应用与多个 Rust/附属进程分别处理平台；能力可能不等价 |
| VCPToolBox | 服务/Web 管理台；Linux amd64/arm64 容器 | 插件式多运行时，平台兼容性按插件变化；CodeSearcher/DailyNoteSearcher 等原生二进制按 win32/linux/darwin × x64/arm64 选择 |

“构建配置存在”与“所有功能在该平台等价”分开处理。Jan 与 Risuai 的移动端、VCPChat 的非 Windows 能力和各项目的原生插件都只完成静态入口确认；没有运行结果时不写成已验证支持。

## 8. 统计边界与复算

本次统一统计由 [`统计仓库.ps1`](统计仓库.ps1) 生成，规则见 [`调查指南.md`](调查指南.md)。复算示例：

```powershell
./统计仓库.ps1 -WorkspaceRoot E:\works\git -Projects @('aio-hub', 'AstrBot')
```

本次对象是工作区内 18 个 AI 项目仓库。`UserScript` 是通用用户脚本集合，未作为 AI 产品项目纳入；`项目调查笔记` 自身也不属于调查对象。统计结果绑定各单项目笔记所列 HEAD 代码快照（jan、Manifold-Desktop、open-webui、SillyTavern 自上一批统计起无提交，数字未变）；项目更新后应重新运行脚本并同步更新单项目笔记与本文。

口径差异：脚本在 GBK 控制台（Windows PowerShell 5.1 默认）下会把含中文路径的跟踪文件解码成乱码而漏计，复算应在 UTF-8 控制台（`[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`）下运行。

aio-hub、VCPChat、VCPToolBox 三行数字按各自单项目笔记为准：

- aio-hub：文档为 `*.md` 口径（372 份，全模式含 `.txt` 为 376）
- VCPChat：源码 598 文件/334,822 行（剔除 vendor/测试/文档）；文档 48/9,919 为 `*.md` 且不含 vendor/测试内；测试 24/6,543 含 `tests/` 与 `SovitsTest/`；vendor 文本 22/42,870 单列
- VCPToolBox：文档 692/161,572 包含 `knowledge/`、`dailynote/` 等文本知识资产

统一脚本口径（UTF-8 运行）对照值：

- aio-hub：源码 2,364/727,324、文档 376/73,778、测试 341/59,547
- VCPChat：源码 536/348,804、文档 64/13,348、测试 20/5,868
- VCPToolBox：源码 1,283/459,877、文档 692/161,572、测试 32/7,066

与笔记的差值为计行方式造成的几十行以内偏差，VCPToolBox 文档数完全一致。
